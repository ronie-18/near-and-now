# Near & Now — Full Codebase Deep Audit (2026-07-16)

Full-codebase pass across all 7 areas: `backend` (Express API), `frontend` (customer website), `admin` (admin panel), Supabase schema/migrations, and all three mobile apps (`near-now-store_owner`, `NAT_Near-Now_Rider-`, `nearandnowcustomerapp`). This supersedes/extends `supabase/SCHEMA_NOTES.md` (schema-focused). `bug_fixes_2026-07-15.md` (the original admin-approval-flow audit) has been merged into this file and deleted — every open item from it that wasn't already fixed or already covered here has been folded into the relevant area section below (marked "from 2026-07-15 audit").

**Reading this doc:** within every `###` header (Critical/High/Medium/Low, etc.), open items are listed first, then a **Fixed** subsection at the bottom of that header holds resolved items (struck through, with the fix summarized). This keeps what's actually still actionable at the top of each section instead of interleaved with history.

**2026-07-16, later pass — cross-checked against `QA_AUDIT.md` and `qa_audit_old.md`** (an older, independent QA pass from 2026-05-26/re-verified 2026-06-16 through 06-23). Every ❌/🟡 finding in both older docs was re-verified against current code and checked against this doc; 58 confirmed-still-open findings that weren't already tracked here were added (each tagged "found via QA-audit cross-check, 2026-07-16" inline, so they're easy to distinguish from this doc's own original findings). Items already fixed since the older audits, or already covered here under different wording, were left out — see each area section for the added items, mixed in with the rest under **Open**.

---

## Session log — 2026-07-17, shopkeeper document verification feature

Deep-dive audit of the shopkeeper app's new "Upload Documents" page (added 2026-07-16, commit `4b48549`), followed by a rebuild and a second bug pass on that rebuild.

**Initial audit findings (all fixed by the rebuild below):** the mobile app's PATCH of a bulk `verification_documents` JSON blob was silently dropped (backend field not whitelisted, DB column never existed) — shopkeepers could "save" documents that never reached the server; the admin panel had no UI at all to view submitted documents before approving a store (see "Store approval is 'blind'" below, now fixed); the `store-documents` storage bucket was never provisioned, and the upload code called `getPublicUrl()` regardless of bucket privacy.

**Rebuild:** replaced the single-JSON-blob design with a proper per-document review workflow — new `store_verification_documents` table (one row per required doc: aadhaar/pan/trade/gst/fssai) instead of a JSONB blob, so admin can approve/reject each document individually with a reason. New backend endpoints for both the shopkeeper (`GET/POST .../store-owner/stores/:id/verification-documents(/:docType)`) and admin (`GET/PATCH .../api/admin/stores/:id/verification-documents(/:docType)`, `requireAdmin`-gated). Document uploads are proxied through the backend (multer, service-role Storage write) rather than direct-from-app to Supabase Storage — the shopkeeper app has no real Supabase Auth session (custom token system instead), so a client-side storage RLS policy couldn't actually be scoped to "only this shopkeeper's own store," and the anon key is baked into every install of the app; routing through the backend (which already authenticates via the existing token system and checks store ownership) avoids exposing every store's Aadhaar/PAN images to anyone who extracts that key. New admin "Review Documents" modal on `StoresPage.tsx`. Shopkeeper app shows a green tick (approved), red badge + rejection reason (rejected), or neutral (pending review) per document, plus PDF upload support (`expo-document-picker`) alongside the existing photo picker, with a 2MB/format disclaimer enforced both client-side and at the Supabase bucket level (`file_size_limit`/`allowed_mime_types`).

**Second bug pass on the rebuild, both fixed same session:**
- **Admin's rejection-reason text field leaked across documents.** `StoresPage.tsx`'s `DocumentReviewModal` kept the reject-reason textarea in a single shared `reason` state var for whichever document's panel was open; clicking "Reject" on a different document without confirming/cancelling the previous one left the old reason text in place — an admin could submit e.g. Aadhaar's rejection reason against the PAN card by mistake. Fixed by clearing `reason` whenever a different document's reject panel is opened.
- **Multer upload errors (e.g. oversized file) bypassed all JSON error handling.** No Express error-handling middleware existed anywhere in `backend/src/server.ts` (confirmed repo-wide); multer's `limits.fileSize` violation calls `next(err)` directly, before the route's own try/catch runs, so a file that slips past client-side size validation (e.g. `expo-image-picker`'s `asset.fileSize` is `undefined` on some devices) got Express's default non-JSON error response instead of a parseable one — the app could only show a generic "Failed to save document," not the real reason. Fixed by adding a catch-all Express error handler that returns proper JSON for `MulterError` (and any other unhandled error).

Not yet done: applying the migration was confirmed done by the user (2026-07-17); still pending is committing/pushing this work and on-device QA of the PDF picker and the 2MB rejection path.

---

## Session log — 2026-07-17, later pass — hardening the verification-documents rebuild

Continuation of the same-day rebuild above. Fixed a chain of missing-grant/RLS gaps discovered live while the user actually tested the feature (the exact "table was simply never grantable" bug class already documented elsewhere in this file, recurring here for a table created earlier the same day): `store_verification_documents` had RLS + a policy but no base `service_role` GRANT (backend calls failed with "permission denied for table"), then, once fixed, no `anon` GRANT either (the admin panel's direct-Supabase "Updated On" bulk read hit the identical error, silently, since it was wrapped in a non-fatal try/catch). Also added the table (plus `stores`, `products`, `customer_orders`, `store_orders`, `order_status_history`, `admin_notifications` — all of which already had realtime subscription code written against them in the apps but were never added to the `supabase_realtime` publication, so those subscriptions had been silently inert this whole time) to the realtime publication.

Feature additions on top of the rebuild: `stores.approved_at`/`approved_by` (who/when the store's final manual approval happened, cleared on revoke) and `store_verification_documents.approved_at`/`approved_by` (who/when a specific document was last approved — unlike `reviewed_at`/`reviewed_by`, deliberately *not* cleared by a later re-upload or rejection, so a document's approval history survives being resubmitted). Admin's Stores table gained an "Approved On" and "Updated On" column (live-updating via the newly-fixed realtime subscription + a 20s poll fallback, since it's unconfirmed whether Realtime's RLS check honors this app's custom `x-admin-token` header the same way normal REST requests do). Dropped `stores.verification_document`/`verification_number` — dead columns from the pre-rebuild single-document flow, confirmed unused anywhere except an unreachable allowlist entry. Raised the size limit from 2MB to 5MB; `file_size` changed from raw bytes to an already-formatted string ("340 KB"/"1.2 MB") computed once at upload time. Added a delete button (removes both the storage object and DB row, so a document can be re-uploaded from scratch) and a camera option (previously photo-library-only), plus a real format check (regex, not just presence) for Aadhaar/PAN/GST/FSSAI numbers — Trade License deliberately excluded, no fixed national format exists for it.

**Two significant bugs found on a final deep-dive pass, both fixed same session:**
- **Document preview silently vanished immediately after a successful save (shopkeeper app).** `app/upload-documents.tsx`'s `saveOne` replaced `serverDocs[key]` outright with the save endpoint's response — which is the raw DB row (`storage_path`, no signed `url`; signed URLs are only ever computed by the separate GET endpoints, generated fresh per-request since the bucket is private). The just-uploaded document's thumbnail blanked back to the empty "Upload X" placeholder right after a successful save, even though the upload had genuinely succeeded, until the screen was remounted and re-fetched real data. Same broken field also corrupted `handleSaveAll`'s completion message, which could say "upload the remaining documents" immediately after every document had just been successfully saved. Fixed by merging the response into the existing document object (keeping whatever `url` was already showing, or falling back to the just-picked local file's own path) instead of replacing it outright.
- **Document preview and Approve/Reject buttons vanished after any admin review action.** Identical root cause in `admin/src/pages/admin/StoresPage.tsx`'s `DocumentReviewModal`: the PATCH (approve/reject) endpoint's raw-row response has no `url` either, and `review()` replaced the local document object with it directly — both the thumbnail and the Approve/Reject buttons (gated on `doc.url` being truthy) disappeared the instant an admin reviewed *any* document, until the modal was closed and reopened. Same merge-instead-of-replace fix.

Both were confirmed by static review (re-reading the actual GET vs. PATCH/POST controller code side by side, not assumption) rather than live-device testing, so on-device/browser QA of both fixes is still outstanding.

**Feature request implemented same session: editing a document after approval suspends the store.** Previously, an already-approved shopkeeper could freely re-upload or delete any of the 5 documents with zero effect on `stores.is_approved` — by original design, per-document review was meant to stay fully independent from the store-level gate. Changed on request: `saveVerificationDocument`/`deleteVerificationDocument` now call a new `suspendStoreIfApproved()` helper that flips `is_approved` back to `false` and clears `approved_at`/`approved_by` (same fields a manual admin revoke clears) whenever the store *was* approved at the time of the edit — a no-op during normal first-time onboarding, since the store isn't approved yet. Both endpoints return a `storeSuspended` flag; the app shows an alert ("sent back for re-verification... contact admin") and redirects to `/pending-verification`.

**One more bug found immediately on deep-diving this new feature, fixed same session:** the redirect above would have been silently undone by client-side caching. `lib/appCache.ts` caches the store list (including `is_approved`) for 10 minutes; neither the save nor delete document calls ever invalidated it. So the instant a shopkeeper got redirected to `/pending-verification`, that screen's own `useStoreApprovalGate("require-pending")` would read the *stale, still-approved* cached store and immediately redirect them straight back into the app — completely undoing the suspension for up to 10 minutes. Fixed by calling the existing `clearStoreCache()` before navigating, forcing a real re-fetch that reflects the just-flipped `is_approved: false`. Not yet verified on-device.

---

## Session log — 2026-07-16, checkout pricing / GST correctness pass

Scoped to `backend/src/services/database.service.ts` (`placeCheckoutOrder`) and `backend/src/services/invoice.service.ts`. Full GST math (forward + reverse, worked examples) now has its own reference doc: **`docs/gst-calculation-explained.md`**.

**Fixed:**
- `placeCheckoutOrder` no longer trusts client-supplied item prices, subtotal, or `payment_status` — see Backend Critical below (item 3 in Top Priority). Went through two passes: the first only used raw `discounted_price` (missed each product's own `gst_rate`); corrected once that per-item GST layer was identified.
- `invoice.service.ts` no longer taxes every line item at a flat hardcoded 5% regardless of the product's real GST rate — see Backend Medium below.

**New findings** (not previously in this doc, surfaced while scoping the two fixes above):
- `delivery_fee` and tip still aren't independently verified server-side in `placeCheckoutOrder` (Medium, bounded — see Backend Medium).
- No admin panel UI exists anywhere to view or set `master_products.gst_rate` (Medium — see Backend Medium).

**Still open, unchanged by this session:**
- Everything else in this document outside the two files above.

---

## 🔴 TOP PRIORITY — fix these first, in roughly this order

### Still open

1. **Customer app: Supabase service-role key is structurally required client-side** (customer app, Critical) — core features (addresses, login) call `assertSupabaseAdminConfigured()`; either the service-role key ships inside every APK (full DB compromise) or these features are broken in production. Needs an architecture fix (route through backend), not a config tweak. (Order placement's piece of this is fixed — see Customer app section below.)
2. **Customer app: full IDOR across profile/orders/addresses** (customer app, Critical) — every Supabase-direct call in `authService.ts`/`addressService.ts` takes a bare `userId` string with zero verification it matches the authenticated session. (Order placement's piece of this is fixed — see Customer app section below.)

> ~~Website double-charges GST on every order~~ — **not a bug.** Confirmed by product owner: the flat 5% added at checkout on top of GST-inclusive product prices is intentional by-design pricing, not double-taxation. See "Confirmed NOT a bug" note under Website frontend below.

Everything else below is organized by area, severity within each area.

### Fixed

1. ~~**`/api/payment/refund` has zero authentication**~~ — **FIXED 2026-07-16**, gated behind `requireAdmin`. `/methods` and `/:paymentId` on the same router are still unauthenticated (see Backend Critical below) — not yet addressed.
2. ~~**Any signed-up delivery partner can hijack/complete/reset ANY order**~~ — **FIXED 2026-07-16**, all four handlers now filter by `assigned_driver_id`/`delivery_partner_id` matching the calling rider.
3. ~~**Checkout order-total/price/payment-status is fully client-trusted**~~ (backend `placeCheckoutOrder`, Critical) — **FIXED 2026-07-16.** Item prices/subtotal are now always recomputed server-side (GST-aware) and `order_total`/`payment_status` are floor-checked/locked — the `order_total: 1, payment_status: "paid"` exploit is closed. The route itself (`POST /api/orders/place`) was unauthenticated at the time this was first fixed; that gap was closed in a later pass the same day when order placement was routed through an authenticated endpoint (`requireCustomer` added, `user_id` now always derived from the session — see Customer app section below). See Backend Critical below for full detail.
4. ~~**Store-owner and delivery-partner signup never verifies OTP actually happened**~~ — **FIXED 2026-07-16.** See Backend Critical below for full detail.
5. ~~**Delivery/pickup OTP and pickup codes are brute-forceable**~~ (backend, High) — **FIXED 2026-07-16.** See Backend High below for full detail.
6. ~~**Tracking endpoints leak any order's address/driver PII to any logged-in customer**~~ (backend, High) — **FIXED 2026-07-16.** See Backend High below for full detail.
7. ~~**Admin login still did client-side `bcrypt.compare()` reading `admins.password_hash` via anon-readable RLS**~~ (admin, Critical) — **FIXED 2026-07-16.** See Admin panel Critical below for full detail. Code-side fix complete; the accompanying RLS migration still needs to be run against the live database (no CLI link in this repo to do it from here).

---

## Pricing model / GST reference (confirmed by product owner, 2026-07-16)

Two independent, stacking GST layers — needed context for anything touching prices, checkout totals, or invoices:

1. **Per-item GST**, baked into each product's sellable price: `item_price = master_products.discounted_price + (discounted_price × master_products.gst_rate / 100)`. `discounted_price` is the pre-tax base. `is_loose = true` (loose/weighed products) → `gst_rate` is always 0. `is_loose = false` (packaged products) → follow the `gst_rate` column. Reference implementation: `frontend/src/utils/priceGst.ts`, mirrored in `admin/src/utils/priceGst.ts` and `frontend/src/services/supabase.ts:206-221`.
2. **A separate flat 5% GST on the whole bill at checkout**, mandated by GoI as a business-level tax (the rate can change if GoI changes it) — applied on top of the already-GST-inclusive item subtotal. This is intentional, not double taxation (see "Confirmed NOT a bug" under Website frontend below). Implemented in `frontend/src/utils/checkoutCalculations.ts`.

Any server-side price/total recomputation must replicate both layers or it will silently diverge from what the customer actually saw and agreed to pay — see the `placeCheckoutOrder` fix above, which initially missed layer 1.

---

## Backend (`backend/src`)

### Critical

**Open:**
- **`requireRider` never checks `is_approved`.** `deliveryPartner.controller.ts:28-58` — only validates the session token exists/isn't expired. `acceptOrder`/`acceptOffer` individually check `is_approved`, but `rejectOrder`/`markPickedUp`/`markDelivered` don't — so a `pending_verification` rider signed up seconds ago can still call those three.
- **`supabaseAdmin` still silently falls back to the anonymous client when the service-role key is missing/malformed** (found via QA-audit cross-check, 2026-07-16). `config/database.ts:61-62` — `export const supabaseAdmin = supabaseServiceKey ? createClient(...) : supabase;`. JWT-role validation was added earlier and does log loud errors on a bad key, but the actual fallback behavior is unchanged: any DB operation that's supposed to bypass RLS with elevated privileges instead silently runs as `anon` and gets RLS-filtered, rather than failing closed/refusing to start. Given how many critical paths depend on `supabaseAdmin` (checkout, payments, admin), a misconfigured key degrades to confusing empty-result bugs instead of a loud startup failure.

**Fixed:**
- ~~No ownership check on order-mutating rider endpoints~~ — **FIXED 2026-07-16**. `acceptOrder` now atomically claims only unassigned orders (409 if already taken) and sets `customer_orders.assigned_driver_id` for consistency with the live `acceptOffer` flow; `rejectOrder`/`markPickedUp`/`markDelivered` now filter every update by the order actually being assigned to `req.riderId`, returning 403 otherwise.
- ~~`processRefund` had zero authentication~~ — **FIXED 2026-07-16**, `/refund` now requires `requireAdmin`. Still open on the same router: `/methods` and `/:paymentId` — `GET /api/payment/methods?user_id=<any-id>` discloses another customer's saved card/UPI details with no auth; `getPaymentDetails` likewise. `/create` wasn't re-evaluated for whether it needs auth (it only creates a pending Razorpay order shell before payment, so may be intentionally reachable pre-login — worth confirming, not assumed safe).
- ~~`placeCheckoutOrder` trusted client-supplied prices/total/payment_status~~ — **FIXED 2026-07-16.** `database.service.ts:722-1034` now looks up each item's real price from `master_products` and overwrites whatever `item.price` the client sent, using the platform's actual pricing formula (confirmed by product owner — see "Pricing model / GST reference" note below): `discounted_price + (discounted_price × gst_rate/100)`, with `gst_rate` forced to 0 for loose products (`is_loose = true`). First pass of this fix (same day) only used raw `discounted_price` with no GST component — corrected once the per-product GST layer was identified. `subtotal_amount` is recomputed from those trusted GST-inclusive prices instead of trusting `orderData.subtotal`; `order_total` must be ≥ `trustedSubtotal × 1.05 + platformFee(9.5) + handlingFee(5.5)` (the flat GoI checkout-level 5% + fixed fees, the portion of the frontend's own total formula that's fully determined by item prices — the demonstrated `order_total: 1` exploit is closed); `payment_status` is now always inserted as `'pending'` regardless of what the client sends, only flippable to `'paid'` via the signature-verified `/api/payment/verify` path. `orders.routes.ts`'s `/place` (the route this all lives behind) was unauthenticated at the time of this fix; that gap was closed in a later pass — see Backend Critical → Fixed → the customer-app order-placement entry. Also see the delivery-fee/tip finding under Medium below.
- ~~**`signupComplete` (store owner + delivery partner) never verifies OTP was actually completed.**~~ — **FIXED 2026-07-16.** `storeOwner.controller.ts:214-312`, `deliveryPartner.controller.ts:91-135` both took the `phone` field on trust with no proof `/api/auth/verify-otp` ran first. Fixed with a signed, short-lived (10 min) "signup ticket": `verifyOTP` (`auth.controller.ts`), on the existing `mode: 'signup'` response for the role-scoped (shopkeeper/delivery_partner) flow, now also returns a `signupTicket` — an HMAC-signed `{phone, role, exp}` blob (new `utils/signupTicket.ts`, `createSignupTicket`/`verifySignupTicket`), signed with a secret that's either `SIGNUP_TICKET_SECRET` or a random value generated at process startup (fine since the ticket only needs to live a few minutes and this is a single-instance deployment — see `AWS_MIGRATION_GUIDE.md`). Both `signupComplete` handlers now reject (403) unless `req.body.signupTicket` verifies against the submitted `phone` and the correct role. Client changes: `near-now-store_owner/app/otp.tsx` and `NAT_Near-Now_Rider-/app/otp.tsx` now thread `signupTicket` through router params to `store-owner-signup.tsx` / `signup.tsx`, which include it in the `signup/complete` POST body. Does not touch the admin-approval gate that runs after account creation (`stores.is_approved` / `delivery_partners.status`) — that's unchanged.
- ~~**`POST /api/orders/place` had zero authentication, trusting `user_id` in the body.**~~ — **FIXED 2026-07-16.** `orders.routes.ts` now requires `requireCustomer` on `/place`; `OrdersController.placeCheckout` always overwrites `req.body.user_id` with `req.customerId` before it reaches `placeCheckoutOrder`, so the client-sent `user_id` is completely ignored. `/create` on the same router remains unauthenticated — not yet addressed (see stale-comment fix, `orders.routes.ts:64-67`, corrected to reflect this).

### High

- **`storeOwner.routes.ts` has no router-level auth middleware**, unlike every other role's router. `deleteStoreProduct` (103-151) looks up `app_users` by session token without filtering `role='shopkeeper'`, unlike the other handlers in the same file — inconsistent auth pattern, latent IDOR if `stores.owner_id` is ever set programmatically for a non-shopkeeper.
- **`cancelOrder` has no order-status guard.** `database.service.ts:419-506` only blocks cancellation if a delivery partner is assigned — never checks if the order is already `order_delivered`. A delivered order can be "cancelled" and auto-refunded (`payment_status === 'paid'` triggers `processRefund`).
- **`updateOrderStatus` (admin) has no state-machine validation.** `orders.controller.ts:205-265` — any admin session can move `order_delivered` back to `pending_at_store`, re-triggering customer notifications inappropriately.
- **Riders can spoof another rider's GPS / write arbitrary order status.** `tracking.controller.ts:107-122` (`updateAgentLocation`) never checks `agentId === req.riderId`. `addTrackingUpdate` (64-87) accepts any status string with no ownership check and no enum validation, writing directly into `customer_orders.status` — despite a code comment explicitly saying this route "must not be open."
- **Driver broadcast dispatch searches for nearby drivers relative to the customer's delivery address, not the store/pickup location** (found via QA-audit cross-check, 2026-07-16). `shopkeeper.controller.ts`'s `broadcastToNearbyDrivers()` (~line 638-646) and `delivery.controller.ts:187-212` both select `customer_orders.delivery_latitude/delivery_longitude` as the search center. A driver "nearby" the delivery address may be far from the store the order needs picking up from first, and vice versa — drivers close to the store but far from the eventual drop-off never get offered the order. Distinct from the already-fixed "no staleness check on driver location" bug in the same function.

**Fixed:**
- ~~**Delivery/pickup OTP and pickup codes are brute-forceable.**~~ — **FIXED 2026-07-16** (tracked as Jira SCRUM-14). `verifyDeliveryOTP` (`deliveryPartner.controller.ts:584`) and `verifyPickupCode` (`:949`) both already checked the order/allocation belongs to the calling rider (`assigned_driver_id === req.riderId`) — no IDOR — but nothing stopped that rider from scripting all 10,000 four-digit combinations with no lockout, since these codes gate "proof of physical handoff." Added two `express-rate-limit` limiters in `deliveryPartner.routes.ts` (same library/pattern already used for login-OTP rate limiting in `auth.routes.ts`): max **3 attempts per 3 minutes**, keyed per `riderId:orderId` (delivery OTP) / `riderId:orderId:allocationId` (pickup code) rather than per-rider-globally or per-IP, so exhausting attempts on one order doesn't lock a rider out of others sharing the same account/network. `skipSuccessfulRequests: true` so only wrong-code (400) responses count — a correct guess or a status-check poll doesn't burn the budget. In-memory store, consistent with this being a confirmed single-instance deployment (same assumption the signup-ticket fix relies on) — no DB migration needed.
- ~~**Tracking endpoints leak PII cross-customer.**~~ — **FIXED 2026-07-16** (tracked as Jira SCRUM-12). `tracking.controller.ts`'s `getOrderTracking`, `getOrderTrackingFull`, `getDriverLocationsForOrder`, `getTrackingHistory` all took only an `orderId` from the URL and never checked it belonged to `req.customerId`, despite every route being `requireCustomer`-gated — that middleware only proves "some valid customer," not "the right one for this order." Since order UUIDs routinely appear in tracking-page URLs/deep-links/API responses, any logged-in customer who observed another customer's order id could pull their full delivery address + GPS coordinates, order total/items, and the assigned rider's name/phone/`vehicle_number`/live location. Fixed by threading `customerId` into all four `database.service.ts` methods: `getOrderTracking` now filters directly on `.eq('customer_id', customerId)`; the other three (which don't query `customer_orders` at all) go through a new `isOrderOwnedByCustomer()` check first and return `null` (→ controller responds 404) if the order isn't theirs. `getOrderTrackingFull` reuses the already-verified order from `getOrderTracking` instead of re-checking, to avoid a redundant query. No route/controller signature changes visible to the frontend — same request/response shape, just correctly scoped now.
  - **Related finding, not yet fixed:** `GET /api/tracking/agents/:agentId/location` (`getAgentLocation`) has the identical bug — `requireCustomer`-gated but takes a bare `agentId` with zero relationship check to any order the caller actually has, so any customer could pull any specific rider's raw live location by id. Confirmed unused by both the website and customer app's real UI (only a legacy/simulator page, `DeliveryPartnerPage.tsx`, hits the sibling PUT endpoint, not this GET) — so it's currently dead-but-exploitable rather than actively relied upon. Worth closing in a follow-up pass; out of scope for this fix since it wasn't part of the originally reported bug and needs its own design decision (the route has no `orderId` to check against at all, so the fix shape is different from the four above).

### Medium

**Open:**
- **`delivery_fee` and tip are still not independently verified server-side in `placeCheckoutOrder`.** `database.service.ts:722-1034` — found 2026-07-16 while scoping the price/total fix above. The client-sent `delivery_fee` is stored as-is; the frontend's real total also adds an optional customer tip that isn't sent as its own field at all (it's pre-folded into `order_total` by `CheckoutPage.tsx`), so it can't be checked apart from the item-price floor already enforced. Worst case is bounded (skip the ₹15-40 distance-tiered delivery charge, or under-total by a manipulated "tip"), materially smaller than the now-fixed near-zero-total exploit. Proper fix needs a frontend contract change (send `tip_amount` separately, let the backend independently compute the distance-tiered delivery fee) before the backend can validate either.
- **No admin panel UI exists to view or set `master_products.gst_rate`.** Confirmed via full grep of `admin/src` (pages, schemas, services) — zero references outside of `admin/src/services/supabase.ts`'s read-only price-display calculation. The column is real, per-product, and admin-controlled in the data model, but in practice can only be set by hand in the Supabase table editor. Likely why most products currently have it unset/0 — worth confirming with product owner whether this needs a real admin form field.
- **Coupon `usage_count` increment is non-atomic (read-then-write race).** `database.service.ts:1199-1215`, acknowledged in its own comment as "unlikely but not impossible" — concurrent checkouts can both pass the usage-limit check before either write lands, letting a `usage_limit: 1` coupon be redeemed multiple times under burst traffic.
- **Google Places/Geocoding/Directions proxy is fully public with no rate limiting.** `places.routes.ts` (all 6 routes) — unlike the OTP endpoints, nothing throttles this; scriptable to run up Google Maps billing (cost-DoS).
- **`getOrderById` returns 500 instead of 404** for a non-existent order (`.single()` throws before the `!order` check can run) — `database.service.ts:402-417` / `orders.controller.ts:188-203`. Worth a broader audit of `.single()` vs `.maybeSingle()` usage.
- **Admin-facing coupon/delivery-partner endpoints have no schema validation** (unlike `orders.routes.ts`, which uses `validate(schema)`) — `coupons.controller.ts:29-49`, `delivery.controller.ts:50-75` pass `req.body` straight to the DB layer.
- **`notifications.controller.ts` has the same IDOR *pattern*** as the tracking bugs (no `req.customerId` cross-check on `userId`/`notificationId`), but is currently harmless because the backing service (`database.service.ts:1668-1687`) is a complete stub (always returns `[]`, no-op writes). Will become exploitable the moment someone implements it without also adding the ownership check the route comment incorrectly claims already exists.
- **No approval notification when admin approves a store or rider** (from 2026-07-15 audit). `notification.service.ts` only covers new-order events (`notifyShopkeeperNewOrder`, `notifyRiderNewOrder`) — approving a store or rider triggers no push/SMS/email; both mobile apps only find out via polling. (No rejection notification needed — there's no rejection state, see the approval-model note in the old 07-15 audit: denied applicants just stay on `is_approved = false` indefinitely.)
- **Legacy `createOrder` hardcodes a flat ₹20/store delivery fee** (found via QA-audit cross-check, 2026-07-16). `orders.controller.ts:89` — `const PER_STORE_DELIVERY_FEE = 20;`, used for every store order regardless of actual distance. This is a distinct, older order-creation path from the `placeCheckoutOrder` flow the delivery-fee finding above already covers — customers routed through this legacy path are charged a fee unrelated to real distance, silently diverging from the site's own ₹15/20/25/40 distance-tier pricing.
- **`GET /api/coupons/` returns every coupon with no active/expiry filter** (found via QA-audit cross-check, 2026-07-16). `coupons.controller.ts:6-14` → `database.service.ts`'s `getCoupons()` (~1103-1110) does a bare `select('*')` with no `is_active`/expiry predicate; the filtered version only exists at the separate `/active` endpoint. Now admin-only (`requireAdmin`), which lowers severity, but admins still see expired/inactive coupons mixed into the main list with no way to tell at a glance.
- **`acceptOffer`'s post-RPC follow-up writes are still two separate, unguarded sequential updates** (found via QA-audit cross-check, 2026-07-16). `deliveryPartner.controller.ts:836` (`customer_orders` update) and `:844` (`store_orders` update) run after the atomic `accept_driver_offer` RPC decides the winner — the RPC itself correctly handles "which rider wins" (already fixed, see Driver broadcast flow below), but these two follow-up writes aren't transactional with each other or the RPC, so a crash between them can leave `customer_orders` and `store_orders` disagreeing about who's assigned.
- **Three independent haversine/distance implementations exist in the backend** (found via QA-audit cross-check, 2026-07-16). `database.service.ts:1307` (`calculateDistance`), `delivery.controller.ts:203` (inline `haversine` closure), `shopkeeper.controller.ts:19` (`haversineKm`). No shared utility — any future fix to distance math (earth-radius constant, unit bug) has to be found and applied in three places independently.
- **`express.json()` has no explicit body size limit** (found via QA-audit cross-check, 2026-07-16). `server.ts:78` — `app.use(express.json())` with no `{ limit: '...' }` option. Relying on Express's implicit default rather than an intentional limit — a plausible DoS vector via oversized payloads on public routes like `/api/places/*` or checkout.
- **`tracking_events` table is fully dead — zero insert call sites anywhere in `backend/src`** (found via QA-audit cross-check, 2026-07-16). Confirmed via full grep — the table exists in migrations and a route theoretically supports it, but only `order_status_history` is actually written. Misleading for anyone building against this table expecting it to be populated.
- **`getNotificationPreferences`/`updateNotificationPreferences` are hardcoded/no-write stubs** (found via QA-audit cross-check, 2026-07-16) — return fixed values / echo input without touching the DB. Narrow, low-traffic, but a real stub alongside the otherwise-real notification system.
- **`getAgentSchedule` ignores its parameters and always returns `[]`** (found via QA-audit cross-check, 2026-07-16) — a stub, not wired to any real schedule data.

**Fixed:**
- ~~**No global Express error-handling middleware**~~ (found via QA-audit cross-check, 2026-07-16) — **FIXED 2026-07-17.** `server.ts` registered no 4-argument `(err, req, res, next)` handler anywhere. Surfaced concretely by the new verification-document upload route: multer's `limits.fileSize` violation calls `next(err)` directly, bypassing the route's own try/catch entirely, so an oversized file that slipped past client-side validation got Express's default non-JSON error page instead of a response the app's error handling could parse into a useful message. Added a catch-all error handler (after all routes, before `app.listen`) that returns `{success:false, error}` JSON for `MulterError` (mapping `LIMIT_FILE_SIZE` to a clear message) and any other unhandled error, instead of Express's default HTML/text response.
- ~~`invoice.service.ts` generated tax invoices using a flat hardcoded rate, not each product's actual `gst_rate`~~ — **FIXED 2026-07-16.** `DEFAULT_GST_RATE = 5` was applied to every line item regardless of what's stored in `master_products.gst_rate` — a product with a non-default per-item GST rate showed one GST-inclusive price to the customer at checkout but got taxed at a different, wrong rate on their official invoice. `fetchOrderData` now joins each `order_item` → `products` → `master_products` to pull the product's real `gst_rate`/`is_loose` (loose products forced to 0, matching the checkout rule); each line reverse-computes its true pre-tax `taxable_value` from the actually-charged `unit_price` using that real rate instead of treating the already-GST-inclusive price as untaxed. The flat GoI checkout-level 5% (layer 2, see "Pricing model / GST reference" above) is now applied once on the item subtotal instead of per-line, folded into the overall `taxable_amount`/`cgst_total`/`sgst_total` the same way platform/handling fee GST already was (no new invoice line) — `grand_total` is unchanged, only the per-line breakdown and CGST/SGST decomposition are now legally accurate. Verified: all 12 `invoice.service.test.ts` tests pass, plus a manual reconciliation (₹100 pre-tax item at 18% GST → ₹118 charged now correctly reverse-decomposes to `taxable_value: 200, cgst: 18, sgst: 18` for a qty-2 line, vs. the old flat-5%-regardless-of-real-rate output). **Not covered by a new automated test** — verified via existing tests + manual calculation only; worth adding a dedicated test for the join + reverse-GST logic if this file gets touched again.

### Low

- **`auth.controller.ts:195,337`** calls `crypto.randomUUID()` without importing `crypto` — works only because Node's global `crypto` happens to be available unprefixed; fragile across environments/bundlers.
- **`notification.service.ts:113-117`** `sendSMS` is a dead stub (`// TODO: Twilio / AWS SNS`) that always returns success without sending anything.
- **`requireShopkeeper` blocks even read-only profile access** for unapproved shopkeepers (`shopkeeper.controller.ts:30-71`) — a newly-signed-up shopkeeper can't check their own approval status.
- **Expo push send calls never inspect per-message error responses** — `delivery.controller.ts:232-241`, `shopkeeper.controller.ts:499-511,550-560` — stale/invalid tokens never get cleaned up, failures are invisible.
- **Duplicate `declare module 'express'` Request augmentation** in `invoice.controller.ts:9-15`, already redundant with 3 other files — the file's own comments document this exact duplication caused a real bug once before.
- **`getProductById` fetches the entire product catalog then `.find()`s in JS** (found via QA-audit cross-check, 2026-07-16). `products.controller.ts:51-66` — `databaseService.getProductsWithDetails()` (no filter) then linear-scans for the one product. Latency/cost concern as the catalog grows.
- **`getNearbyStores` does a full table scan + JS-side haversine filter** (found via QA-audit cross-check, 2026-07-16). `database.service.ts:283-298` — `select('*').eq('is_active', true)` over all stores, then filtered by distance in JS. No PostGIS/SQL-side geo filter, on a hot customer-facing path.
- **`getProductsWithDetails` does the same full-scan + JS-side radius filter** (found via QA-audit cross-check, 2026-07-16). `database.service.ts:245-281` — queries unfiltered by location, then filters by `calculateDistance()` in JS when a radius is requested.

---

## Multi-store order dispatch (`order_store_allocations` / `driver_order_offers`)

Found and fixed in a separate, targeted pass (not part of the original 7-area sweep above) while verifying the partial-acceptance → reallocation → driver-assignment flow end to end. All seven are now fixed — no open items in this section.

### Reallocation flow (`shopkeeper.controller.ts`)

- ~~**Order permanently stalls if no nearby store stocks a reallocated item.**~~ — **FIXED 2026-07-16.** `reallocateMissingItems` only ever searched a fixed 4km radius and, on failure, marked the item `unavailable` with no path forward — the order stayed stuck in `store_accepted` forever since nothing re-checked whether it could still proceed. Now searches 4km, then widens to 8km (never further) for whatever's left; if still unresolved, flags the item for an admin-approved refund (new `admin_notifications` row, type `refund_required`, with the computed line-item amount and Razorpay payment ID) and lets the rest of the order proceed to dispatch instead of stalling. New admin panel action: "Refund ₹X" button on the flagged notification (`NotificationsPage.tsx`), backed by `POST /api/payment/resolve-item-refund/:notificationId` (`requireAdmin`-gated, validates against `customer_orders.refunded_amount` so a refund can never exceed what was actually paid). Migration: `20260719000000_customer_orders_refunded_amount.sql`.
- ~~**Order permanently stalls if the reallocated store never responds.**~~ — **FIXED 2026-07-16.** A newly-created `pending_acceptance` allocation had no timeout — if that shopkeeper never opened the app, the order sat waiting indefinitely. New `expireStaleAllocations(orderId)` runs opportunistically off `GET /api/tracking/orders/:orderId/full` (the endpoint the customer app already polls while an order is active) — any allocation still `pending_acceptance` after 5 minutes is auto-rejected and re-offered to the next nearest store via the same reallocation search. No new cron/infra needed.
- ~~**Race condition: two stores accepting near-simultaneously could double-broadcast to drivers.**~~ — **FIXED 2026-07-16.** The "is anything else still pending?" check was a plain read-then-write with no locking, so two concurrent acceptances could both conclude "I'm last" and both call `broadcastToNearbyDrivers` (mostly harmless in practice — the driver-offer upsert dedupes — but caused duplicate push notifications and a duplicate `order_status_history` row). Now atomic: new Postgres function `finalize_order_if_ready(p_order_id)` (migration `20260720000000_finalize_order_if_ready.sql`) locks the `customer_orders` row `FOR UPDATE` before deciding, mirroring the same pattern already used by `accept_driver_offer`.

### Driver broadcast flow (`shopkeeper.controller.ts` / rider app)

- ~~**Offline or suspended drivers could still accept a stale pending offer.**~~ — **FIXED 2026-07-16.** `acceptOffer` and the legacy `acceptOrder` only checked `is_approved`, never `is_online`/`status`, and neither did the `accept_driver_offer` Postgres function itself — so a driver who went offline or got suspended after receiving an offer could still accept it. Fixed at the real point of authority: `accept_driver_offer` now checks `is_approved`, `is_online`, and `status = 'active'` inside the same row-locked transaction (migration `20260721000000_accept_driver_offer_eligibility_check.sql`), returning `'driver_not_eligible'` if not. Both Node endpoints also fast-fail with a friendly 403 before calling the RPC.
- ~~**No cap on how many drivers get offered one order.**~~ — **FIXED 2026-07-16.** `broadcastToNearbyDrivers` offered every online/active driver within 10km with no limit — a busy area could push-notify 50+ drivers for one order. Now sorts candidates by distance (already computed, previously discarded) and caps the broadcast to the nearest 20 (`MAX_DRIVERS_PER_BROADCAST`).
- ~~**No staleness check on driver location.**~~ — **FIXED 2026-07-16.** Neither `broadcastToNearbyDrivers` nor `dispatchReadyOrdersToDriver` filtered on how recently `driver_locations` was updated — a driver marked `is_online=true` but whose app crashed/was killed hours ago would still be treated as "nearby" based on stale coordinates. Both now filter to locations updated within the last 5 minutes (`DRIVER_LOCATION_STALE_MS`).
- ~~**Up to 15s lag before an already-accepted order disappears from other drivers' lists.**~~ — **FIXED 2026-07-16 (rider app).** The rider app only had a Realtime subscription for new offer `INSERT`s, not `UPDATE`s — when another driver won an order, the loser's app didn't find out until its next 15s poll, showing a stale "available" card. `NAT_Near-Now_Rider-/app/(tabs)/home.tsx` now also subscribes to `UPDATE` events on `driver_order_offers` and removes the offer from local state the instant its status leaves `pending`.

---

## Website frontend (`frontend/src`)

### Confirmed NOT a bug

- **Flat 5% added at checkout on top of GST-inclusive product prices** (`utils/checkoutCalculations.ts:88`, `pages/CheckoutPage.tsx:426-427,603-604`) — flagged in the initial pass as a "double GST charge." **Confirmed by product owner: this is intentional by-design pricing, not a bug.** No code change made.

### Critical

- **Client fully computes the amount sent to Razorpay, with no visible server-side recheck.** `pages/CheckoutPage.tsx:427` → `services/paymentGateway.ts:76-89` posts a client-computed `amount` to `/api/payment/create`. Combined with the backend's `placeCheckoutOrder` trusting client-supplied totals (see Backend Critical above), there's no verified guardrail preventing a manipulated total from reaching payment creation.
- **Direct-from-browser order-write fallback path.** `utils/apiBase.ts:18-21` (`shouldUseBackendApi`) + `services/supabase.ts:483-750` (`createOrder`) — if `VITE_API_URL` is ever unset in a production build, the app falls back to writing `customer_orders`/`store_orders`/`order_items` directly from the browser via an anon-key client, letting the client set `order_total`/`discount_amount`/`payment_status` directly, relying entirely on unverified RLS to prevent abuse.

### High

- **Logout doesn't clear the cart.** `context/AuthContext.tsx:166-186` (`logoutUser`) never calls `clearCart()` — confirmed via grep that no logout call site does. Shared/kiosk device: User B logs in after User A logs out and sees/can check out User A's cart. `LocationContext`'s saved location is likewise never cleared.
- **No global 401 handling on the customer-facing site.** Only `ShopkeeperApp.tsx`/`DriverApp.tsx` (separate legacy pages?) check `status === 401`; the actual customer flow (`authService.ts`, `orderService.ts`, `supabase.ts` backend-API branches) never detects an expired/revoked session, clears it, and redirects to login — user gets stuck with `isAuthenticated: true` but every API call silently failing.
- **`useOrderTrackingRealtime.ts:64-96,99-147` — stale closure bug.** The realtime-subscription effect's dependency array is `[orderId, !!order]` (a boolean), not `[orderId, order]`, so the closure keeps referencing the *first* render's `order` object for the subscription's lifetime. Any order field not explicitly re-assigned in each update permanently reverts to its first-render value and never updates again.
- **GSTIN/business name captured but silently discarded from the website's own order payload** (found via QA-audit cross-check, 2026-07-16 — distinct from the customer *mobile app*'s version of this, already fixed). `pages/CheckoutPage.tsx:80-82` (state) / `:451-474` (`orderData` — no `gstin`/`businessName` field present). Business customers filling this in get no GST record on their invoice.
- **Invoice download sends the customer's raw UUID as a Bearer token** (found via QA-audit cross-check, 2026-07-16). `pages/OrdersPage.tsx:251-254` — `Authorization: Bearer ${user.id}`. Fragile/misleading auth pattern on a live call site; would be directly exploitable if `requireCustomer` ever regressed to accepting bare UUIDs.
- **Realtime subscription + an unconditional 3s poll run simultaneously** (found via QA-audit cross-check, 2026-07-16 — distinct file from the stale-closure bug above, though same general area). `hooks/useOrderTrackingRealtime.ts:141` — `setInterval(refreshOrderAndHistory, 3000)` runs alongside the realtime channel with no health-gating, doubling backend load for no benefit when realtime is healthy.
- **No `AbortController` on in-flight tracking fetches** (found via QA-audit cross-check, 2026-07-16). `useOrderTrackingRealtime.ts:64-96,154-165` — stale responses can land and overwrite state after unmount/navigation or after a newer request already resolved.
- **Driver location polled every 2s unconditionally** (found via QA-audit cross-check, 2026-07-16). `useOrderTrackingRealtime.ts:151-165`, no gating on realtime connection state — the website's own version of an already-documented customer-mobile-app bug.

### Medium

- **`NotificationContext.tsx:34-48`** — auto-dismiss timers reset for *all* visible toasts every time the notifications array changes (add or remove), so bursts of notifications within the dismiss window can pile up or overstay far longer than configured.
- **`HomePage.tsx`** never re-fetches products when the user's delivery location changes (unlike `ShopPage.tsx`, which correctly does) — stale "nearby store" listing until a full reload.
- **`SearchPage.tsx:15-39`** — no request cancellation/staleness guard; rapid searches can have an older, slower response overwrite a newer search's results.
- **`ThankYouPage.tsx:10-17,131-158`** — order details come only from React Router `location.state` with no fallback fetch by order ID; refreshing the post-payment Thank You page (very common — checking email, app-switching) loses all context and shows "Order ID: N/A".
- **Two parallel, inconsistent admin-auth systems** in the same codebase — `secureAdminAuth.ts`'s Edge-Function JWT login is referenced by several components but the real login flow (`AdminLoginPage.tsx`) uses a completely different mechanism (`adminAuthService.ts`), meaning the "secure" path is dead code that's easy to mistakenly believe is what's actually running (this is the same root confusion behind the still-live client-side-bcrypt admin login bug in the Top Priority list).
- **API-base-URL resolution logic duplicated in 3 places** (`utils/apiBase.ts`, `services/authService.ts:44-52`, `services/orderService.ts:4-12`) instead of sharing one implementation — drift risk.
- **`utils/encryption.ts`** — dead code, unused anywhere; client-side "encryption" with a bundle-embedded key (defaulting to a hardcoded weak string if unset) provides no real confidentiality even if it were wired up.
- **Checkout email field is `required`** (found via QA-audit cross-check, 2026-07-16). `pages/CheckoutPage.tsx:813` — blocks OTP customers with no real email from completing checkout, even though `customer_email` is optional server-side.
- **No empty-cart redirect at checkout** (found via QA-audit cross-check, 2026-07-16). `CheckoutPage.tsx` has no mount-time guard, only a submit-time check and a render-time message in the summary panel — landing directly on `/checkout` with an empty cart shows a fully rendered, unusable form instead of redirecting.
- **`getCurrentUserFromSession` still uses the anon Supabase client and is blocked by RLS** (found via QA-audit cross-check, 2026-07-16). `services/authService.ts:183-215`. Masked for the common case by `AuthContext.tsx` restoring from `localStorage` on mount, but any path needing a genuine fresh server read silently falls back to stale local state.
- **`CartContext.addToCart` mutates the cart item object in place** (found via QA-audit cross-check, 2026-07-16). `context/CartContext.tsx:121-125` — `updatedItems[existingItemIndex].quantity += quantity` mutates the same object referenced in `prevItems`, even though the array itself is a new reference. Latent bug for any `React.memo`-wrapped cart row.
- **OrderTrackingPage "Invoice" link points to a non-existent route** (found via QA-audit cross-check, 2026-07-16). `pages/OrderTrackingPage.tsx:738-746` links to `/api/invoices/${orderId}` (doesn't exist); real routes are `/api/invoices/order/:orderId/{customer,store,delivery}`. `OrdersPage.tsx:252` calls the correct path, proving this is an inconsistency, not a design choice. Always 404s.
- **`components/ProtectedRoute.tsx` is dead code** (found via QA-audit cross-check, 2026-07-16) — no importer anywhere; `CheckoutPage`, `OrdersPage`, and others each hand-roll a slightly different inline auth-redirect guard instead, producing inconsistent UX for the same guard condition.
- **Stale/undocumented Vite env vars** (found via QA-audit cross-check, 2026-07-16) — root `.env.example:7` still documents `VITE_SUPABASE_SERVICE_ROLE_KEY`, which nothing in `frontend/src` reads anymore (misleading operators toward re-introducing the already-fixed leaked-key risk); `VITE_ENCRYPTION_KEY` (read by the dead-code `utils/encryption.ts:5`) is undocumented anywhere.
- **GSTIN "claim GST credit" UI copy is misleading** (found via QA-audit cross-check, 2026-07-16). `CheckoutPage.tsx:1100` — "Claim GST credit up to 18% on the order," promising a benefit that can't happen since GSTIN is dropped from the payload (see the High-severity GSTIN finding above). Real tax/accounting consequence for business customers.

### Low

- Greedy store-assignment algorithm in `createOrder`'s fallback path uses `items.indexOf()` inside nested loops — effectively O(n³) for cart size n (harmless at typical cart sizes, but wasteful).
- 60+ files contain unstripped `console.log`/`console.error` in production, including full order payloads and user IDs/phones.
- `utils/dataMasking.ts` — confirmed dead code, zero importers.
- Security utilities (`sanitize.ts`, `csrf.ts`, `rateLimit.ts`) are applied only to the admin surface, never to customer-facing forms — inconsistent application, though no direct injection sink was found on the customer side.

---

## Admin panel (`admin/src`)

### Critical

- **`getAdminClient()` silently downgrades to the anonymous client on any missing/expired token — for every call site, not just login.** `services/supabase.ts:46-55`. Every function in `adminAuthService.ts`, `adminService.ts`, `auditLog.ts`, and every page calling `getAdminClient()` directly runs as fully anonymous the moment the session token is gone, with no error thrown — just silently executes against whatever RLS allows `anon` to do. The `admins`/`admin_sessions` RLS gap this used to compound is now closed (see Fixed below), but this general fail-silent-to-anon pattern is still live for every other admin-panel table.
- **Admin creation has no server-side *role* check** (found via QA-audit cross-check, 2026-07-16). `createAdmin()` (`adminAuthService.ts:190-227`) does a plain insert with nothing at the DB layer stopping a legitimately-authenticated but low-privilege admin (e.g. `viewer`) from assigning themselves or anyone else the `super_admin` role. **Partially improved 2026-07-16** (see Fixed below): the table now has RLS requiring a valid admin session at all (previously anyone, authenticated or not, could write to it), but that check doesn't look at *which* role is asking — this remaining gap is a role/permission check, not an RLS-existence one.

**Fixed:**
- ~~**Store approval is "blind"**~~ (from 2026-07-15 audit) — **FIXED 2026-07-17.** `StoresPage.tsx` never selected/rendered `verification_document`, `verification_number`, or `owner_image_url` — admin could click Approve without ever seeing the shopkeeper's submitted ID. Fixed as part of the per-document verification rebuild (see 2026-07-17 session log at top): a new "Review Documents" modal shows each of the 5 required documents (thumbnail/PDF link, number, status) with per-document approve/reject, backed by new `requireAdmin`-gated endpoints. The overall store `is_approved` toggle stays a separate manual action, unaffected.
- ~~**Client-side login read `admins.password_hash` via an anon-readable RLS policy — and there was no RLS on `admins`/`admin_sessions` at all.**~~ — **FIXED 2026-07-16** (tracked as Jira SCRUM-69). `services/adminAuthService.ts`'s `authenticateAdmin` (the function actually wired to the login form) called `getAdminClient()` *before* any session exists, which falls back to the plain anon-key client, then ran `bcrypt.compare()` **in the browser** against a hash fetched over the public anon key. Investigating further turned up something worse than the original write-up: grepping every migration for `ALTER TABLE public.admins ENABLE ROW LEVEL SECURITY` returned zero hits, and a base anon `GRANT` on both `admins` and `admin_sessions` was confirmed already present (`20260718000000_fix_admin_sessions_grants.sql`). That combination meant **anyone with the public anon key could `SELECT * FROM admins` directly** (no login needed) — and worse, since `authenticateAdmin` also generates its session token client-side (`crypto.randomUUID()`) and INSERTs it into `admin_sessions` itself, **anyone could skip the password check entirely** and forge a valid admin session by inserting an arbitrary `{admin_id, session_token}` row directly via the anon-key REST API. Fixed in two parts: (1) new backend endpoint `POST /api/admin/login` (`backend/src/controllers/admin.controller.ts`, `backend/src/routes/admin.routes.ts`, rate-limited 10/15min like the other login endpoints) does the `admins` lookup + `bcrypt.compare()` server-side via the real service-role client — `password_hash` never reaches the browser, and the session token/`admin_sessions` row are both created server-side. `adminAuthService.ts`'s `authenticateAdmin` now just calls this endpoint and keeps the same `sessionStorage` shape, so nothing downstream (`getAdminClient()`, `is_admin_authenticated()`) needed to change. (2) New migration `20260722000000_admins_admin_sessions_rls.sql` enables RLS on both `admins` and `admin_sessions` with an `admin_full_access` policy gated on `is_admin_authenticated()` — the same pattern already used for every other admin-facing table (`customer_orders`, `stores`, etc.) — which closes the direct-Supabase forgery path regardless of what the frontend does. `is_admin_authenticated()` is `SECURITY DEFINER` and reads `admin_sessions` internally with RLS bypassed, so it isn't affected by (and doesn't recurse through) the new policy on that same table. Typechecks clean on both `backend` and `admin`. **Not yet applied to the live database** — this repo has no `supabase/config.toml`/CLI link, so the migration needs to be run via the Supabase dashboard SQL editor or `supabase db push` by whoever has project access; the code-side fix is complete but the DB-side RLS half is inert until that migration actually runs.

### High

- **Same unchecked-mutation bug class as the already-fixed `StoresPage.toggleApproval`, still present in `NotificationsPage.tsx` and `AdminHeader.tsx`.** `NotificationsPage.tsx:221-239` (`markAllRead`/`markOneRead`/`deleteNotification`) and `:76-82` (`PushNotificationPanel.handleSend`), plus `AdminHeader.tsx:118-120` (`markAllRead`) — all call `.update()`/`.delete()`/`.insert()` against `admin_notifications` with **no error or row-count check**, then optimistically update local state regardless of whether the DB call actually succeeded. A stale/expired session makes these silently no-op while the UI reports success.
- **`PushNotificationPanel.handleSend` never actually sends a push — it only logs a fake-success row** (found via QA-audit cross-check, 2026-07-16 — a distinct defect from the unchecked-mutation bug listed above for the same function). `NotificationsPage.tsx` `handleSend` (~lines 57-100) fetches Expo push tokens, then only inserts an `admin_notifications` row and reports success to the admin — there is no actual Expo push API call anywhere in the function. Admins believe a broadcast went out to devices when none did.
- **No per-endpoint/server-side permission enforcement for admin roles** (found via QA-audit cross-check, 2026-07-16). `hasPermission()` (`adminAuthService.ts:293-309`) is only checked from React components; nothing at the RLS or backend-middleware layer enforces role. A `viewer`-role admin can bypass the UI (devtools/direct Supabase calls) to perform writes their role shouldn't allow.
- **Password-strength schemas defined but never used** (found via QA-audit cross-check, 2026-07-16). `admin/src/schemas/admin.schema.ts:11-39,41-68` (`CreateAdminSchema`/`UpdateAdminSchema`) enforce upper/lower/digit/special-char, but `CreateAdminPage.tsx:65` / `EditAdminPage.tsx:110` only check `length < 8` — weak 8-character passwords like `aaaaaaaa` are accepted for admin accounts.

### Medium

- **Session-creation writes in `authenticateAdmin` are unchecked.** `adminAuthService.ts:112-127` — the `admin_sessions` insert and `last_login_at` update results are discarded entirely (not even logged). If the session insert silently fails, the UI hands back a token that *looks* valid but has no corresponding DB row — every subsequent `is_admin_authenticated()` check fails, and the admin appears logged in while every real page shows empty data (this is precisely the failure mode already root-caused and fixed once on 2026-07-15 for the *first* login after that fix — this finding is about the same class of bug being able to recur silently for any future login where the insert happens to fail).
- **`adminService.ts:802-811` (`updateCustomerStatus`) is a dead no-op** — takes unused params, logs a warning, returns `null` without touching the DB. Currently unused by any button, but a trap if resurrected.
- **`AdminSidebar.tsx`'s `isActive('/')` check references a `/admin` path that doesn't exist** anywhere in the app's actual routing (mounted at `/`) — the Dashboard nav item can never show as active. Cosmetic, but the kind of leftover-after-refactor bug worth cleaning up.
- **`navigator.userAgent` called unguarded in `auditLog.ts`** (found via QA-audit cross-check, 2026-07-16). `services/auditLog.ts:37,62,78` all call `navigator.userAgent` directly with no environment guard (unlike `adminAuthService.ts:119`, which already has one). If ever run outside a browser context, throws and silently drops the audit-log write (caught by an outer try/catch, but the security event is lost).
- **`getCustomers` returns all `app_users` rows with no role filter** (found via QA-audit cross-check, 2026-07-16). `adminService.ts:703-706`, no `.eq('role', 'customer')` — non-customer roles would leak into the Customers list/count if `app_users` ever holds them in a way that matters.
- **Status mapping collapses order statuses into 5 states** (found via QA-audit cross-check, 2026-07-16). `mapDbStatusToFrontend`, `adminService.ts:447-454` — e.g. `preparing_order` and `ready_for_pickup` both collapse to `'confirmed'`, losing operationally-relevant granularity in the admin UI.
- **`updateOrderStatus` (admin) can never explicitly set `preparing_order`** (found via QA-audit cross-check, 2026-07-16 — distinct from the already-documented missing state-machine-validation bug in the same function). `adminService.ts:662-668` maps `confirmed` → `store_accepted`, skipping over `preparing_order` entirely.
- **Admin session stored in `sessionStorage` only, no persistent option** (found via QA-audit cross-check, 2026-07-16). `AdminLoginPage.tsx:46-48` — closing the tab silently logs the admin out.
- **`SettingsPage` notification/appearance prefs are localStorage-only, don't follow the admin across devices** (found via QA-audit cross-check, 2026-07-16). `SettingsPage.tsx` (`NOTIF_PREFS_KEY` lines ~215-268, `DISPLAY_PREFS_KEY` lines ~272-339) — only the Account/password tab does a real DB write via `updateAdmin()`.

### Low

- Client-side-only rate limiting (`utils/rateLimit.ts`) — an in-memory Map reset by any page refresh, providing no real protection for the vulnerable login flow above.
- Admin email/role/error-stack details logged to console on every login attempt, uncompressed for production.
- `services/supabase.ts` inside the *admin* app contains a large amount of customer-facing logic (`createOrder`, `getUserOrders`, newsletter, address CRUD) that's dead weight in the admin bundle — looks like a stale copy-paste from the customer-facing frontend.
- **`sessionStorage['adminData']` parsed with raw, uncaught `JSON.parse` in 3 components instead of the shared `getCurrentAdmin()` helper** (found via QA-audit cross-check, 2026-07-16). `CreateAdminPage.tsx:33-34`, `EditAdminPage.tsx:38-39`, `AdminManagementPage.tsx:125-126` — any corruption/tampering of that storage key crashes these pages with an unhandled exception instead of failing gracefully.

### Confirmed NOT bugs (verified during this pass)

- All backend-API `fetch()` calls needing admin auth correctly attach the `Authorization` header now.
- All `.from()` calls in pages/services (aside from the pre-login fallback above) correctly use `getAdminClient()`.
- `NotificationProvider` and `ErrorBoundary` are both correctly mounted (fixed earlier today).

---

## Supabase schema & migrations

### Critical

- **`supabase/migrations/` cannot bootstrap this schema from an empty database — this is systemic, not isolated to `admin_sessions`.** Grepping every `CREATE TABLE` for `coupons`, `app_users`, `stores`, `admins`, `categories`, `master_products`, `order_items`, `store_orders`, `coupon_redemptions`, `product_images`, `product_attributes`, `product_details`, `product_reviews`, `audit_logs`, `security_events`, `failed_login_attempts`, `customers`, `driver_locations`, `admin_activity_logs`, `inventory_logs` across every migration file returns **zero matches for all of them** — only `delivery_partners` has a real tracked `CREATE TABLE`. `supabase/SCHEMA_NOTES.md` currently frames the untracked-table problem as isolated to `admin_sessions`; it should be corrected to say this is the norm for this repo, not an exception. Practical impact: `supabase db reset` or spinning up a fresh project from these migrations alone would fail immediately.
- **`customer_orders` (`20260404120000_customer_orders_razorpay_payment_id.sql:109`) declares its `payment_method` column as type `public.payment_method_type` — a type that is never `CREATE TYPE`'d anywhere.** The enum actually created later (`20260405000000_..._enums_payouts.sql:38`) is named `public.payment_method` (no `_type` suffix). A later migration even has defensive code detecting a mismatched `udt_name`, implicitly acknowledging this — but that defensive code only works if `payment_method_type` already exists out-of-band in the live DB. Replayed purely from tracked migrations, this fails with `type "public.payment_method_type" does not exist`.
- **`public.order_status` — the single most important enum in the app (drives all order lifecycle transitions) — has no `CREATE TYPE` anywhere in migrations either.** Used from `20260404120000` onward and later `ALTER TYPE ... ADD VALUE`'d, but never created.

### High

- **Every `SECURITY DEFINER` function in the migration history is missing `SET search_path = public`.** Full list: `accept_driver_offer()`, `auto_offline_stale_drivers()`, `is_account_locked()`, `is_admin_authenticated()` (re-created on 2026-07-17 still without the fix), `notify_admin_new_order()`, `notify_admin_new_user()`, `debug_admin_headers()`. `is_admin_authenticated()` is the most sensitive — it's the sole gate for every `admin_full_access` RLS policy across 9 tables. This is a known, standard Postgres privilege-escalation hardening gap.
- **`debug_admin_headers()` — the temporary diagnostic function from the 2026-07-17 investigation — was never dropped**, despite its own comment saying "safe to drop once the admin-auth 0-rows issue is resolved" (which it has been, as of the very next day's migrations). It's `SECURITY DEFINER`, has no `search_path` pin, is granted to `anon`/`authenticated`, and returns the full matching `admin_sessions` row (including `session_token`) to any caller who can supply a valid `x-admin-token`. Should be dropped or restricted to `service_role` only.

### Medium

- **No DB-level enforcement forcing `stores.is_active` back to `false` if admin revokes `stores.is_approved` on an already-online store** (from 2026-07-15 audit). The rider side has an equivalent trigger (`20260428000001_decouple_is_online_from_status.sql`, forces `is_online = false` whenever `status <> 'active'`) — the store side has no matching trigger, so a store stays online until the shopkeeper (or some future toggle attempt) happens to touch the row.

### Low / informational

- The `coupons`/`product_images`/`product_attributes`/`product_details` grants added on 2026-07-18 (`SELECT` to `anon`/`authenticated` while RLS stays disabled on those tables) is exactly the trap `SCHEMA_NOTES.md` itself warns about — appears deliberate and carefully scoped (non-PII columns only, `product_reviews` explicitly excluded for its PII columns), but worth a second confirmation pass that RLS staying disabled there is intentional, and updating SCHEMA_NOTES to note the trap has since been (carefully) sprung rather than just theoretical.
- Three RLS policies (`customer_own_orders`, `customer_own_record`, `partner_own_record`) are permanently dead code — they gate on `auth.uid()`, but this app doesn't use Supabase Auth (uses its own phone-OTP JWT), so `auth.uid()` is always NULL from the frontend, documented in an unrelated migration's own comment. Not exploitable (fails closed), but misleading — a future developer could believe there's a working customer-self-access RLS path when there isn't.

---

## Store-owner app (`near-now-store_owner`)

### Critical

- **Push notification service is never initialized at app startup — notifications are effectively opt-in-and-hidden, not opt-out.** `lib/notifications.ts`'s `notificationService.initialize()` has no caller anywhere in the app (confirmed via full-repo grep) — the only way a token gets registered is if the shopkeeper manually opens Settings → Notifications → taps "Enable," despite the in-app copy implying it's already on by default. A shopkeeper who never visits that screen will never receive a push for new orders, silently missing them if they're not actively watching the app.
- **No global 401 handling — same bug class already flagged (and left unfixed) in the customer app.** `lib/api-client.ts`'s `request()` never inspects 401s to trigger logout; `error-handler.ts` has a `handleAuthError()` that's dead code (never called). Every screen just shows empty data indefinitely on an expired/revoked session, with no path back to login short of manually finding Settings → Logout.

### High

- **`clearSession()` doesn't clear the store-list cache — real cross-account data leak window on shared devices.** `session.ts:56-63` never calls `clearStoreCache()` from `lib/appCache.ts`, which owns a 10-minute in-memory + AsyncStorage cache. If shopkeeper B logs in within 10 minutes of shopkeeper A logging out on the same device, `home.tsx`/`previous-orders.tsx`/`payments.tsx` will briefly render **A's** store name/address/approval status before B's real fetch overwrites it.
- **Push token is never cleared on logout or re-associated on a different user's login** — same root cause as the initialize() gap above; a shared device could keep delivering one shopkeeper's order notifications to whoever's currently logged in.
- **`useRequireStoreApproval()` is reactive, not preventive.** The redirect only fires after an async network round-trip resolves; meanwhile the Orders/Payouts/Inventory screens' own data-fetching effects run in parallel, unconditioned on the approval check — an unapproved shopkeeper can view/accept/reject real orders and edit inventory for the duration of that request, and if the request itself errors out, the guard fails open **silently forever** (never redirects at all).
- **`app/inventory.tsx` — an orphaned legacy route with no approval guard at all**, still deep-link-reachable via Expo Router's automatic file-based routing even though no in-app navigation targets it; writes directly to the DB via `lib/storeProducts.ts`.

**Fixed:**
- ~~**OTP autofill silently broke if focus was on any box other than the first when the code arrived.**~~ — **FIXED 2026-07-17.** `app/otp.tsx`'s 6 OTP boxes all had `textContentType="oneTimeCode"` unconditionally (the prop that triggers iOS's "Code from Messages" autofill suggestion) — not scoped to just the first box — but only box 0 had `maxLength={6}` to actually hold the full code; boxes 1–5 were capped at `maxLength={1}`. If the shopkeeper had tapped into any box other than the first (e.g. to fix a typo) before using the autofill suggestion, the full 6-digit code landed in a 1-character box and was silently truncated to a single digit, with no error shown. Confirmed by diffing against the rider app's `otp.tsx` (identical autofill setup, no reported issue) — it has no `maxLength` restriction on any box at all, relying entirely on its (also-present-here) multi-character redistribution logic in `handleDigitChange` to correctly split a paste/autofill across all 6 boxes regardless of which one receives it. Removed the `maxLength` restriction to match; also added `selectTextOnFocus` for the same robustness the rider app already had.
- ~~**Android hardware back button unresponsive while a modal is open, and auto-rotate wasn't actually disabled despite `app.config.js` already saying `orientation: "portrait"`.**~~ — **FIXED 2026-07-16** (reported directly, tracked as Jira SCRUM-3; not from this doc's own audit passes). Two separate root causes: (1) Neither `<Modal>` in the app set `onRequestClose`, which Android's RN `Modal` requires to respond to the hardware back button — pressing back while the store online/offline confirm sheet (`app/(tabs)/home.tsx:468`) or the document-type picker (`app/profile.tsx:498`) was open did nothing, trapping the user until they manually tapped Cancel/the backdrop. Added `onRequestClose` to both (guarded on the confirm sheet so it won't close mid-request, matching its existing Cancel-button `disabled` behavior). (2) `app.config.js:17`'s `orientation: "portrait"` was correct at the JS level, but `android/app/src/main/AndroidManifest.xml:30`'s `MainActivity` still had `android:screenOrientation="fullSensor"` — since `android/` is a committed prebuild (not regenerated via `expo prebuild --clean`), the stale native manifest value won at runtime and the app kept auto-rotating regardless of the config. Changed to `portrait` to match. Left the ML Kit barcode-scanner activity's own `screenOrientation="fullSensor"` untouched (separate third-party activity, out of scope). Changes are applied locally but not yet committed/built into an APK for on-device QA — Jira ticket left in **In Review**, not Done, pending that verification.

### Medium

- Realtime channel names include `Date.now()` (`home.tsx:216,223`), causing churn on every store-selection/re-render instead of reusing/deduping channels — confirmed still present from a prior audit.
- Client-side "auto-complete order after 2 minutes" logic (`previous-orders.tsx:285-305`) only runs while the app happens to be open and polling — if the shopkeeper backgrounds/closes the app, orders can be stuck un-completed indefinitely; this should be a server-side scheduled job, not client-triggered.
- Two entirely dead-code order-management implementations exist in parallel with the real one: `hooks/useOrders.ts` + `components/IncomingOrderPopup.tsx` (a more complete 60s-auto-reject flow, never wired in) and `lib/order-service.ts` + `components/OrderFilters.tsx` (targets a `/store-owner/orders/*` API contract that doesn't match the real `/shopkeeper/allocations/*` endpoints in use — would silently fail if ever revived).
- `context/AppContext.tsx` and `lib/testSupabase.ts` (including a function that performs live inserts/deletes against the real `products` table) are both entirely dead/unreferenced code.
- `CachedStore` type doesn't declare `is_approved`, so all the approval-gate logic throughout the app relies on untyped `as any` casts — a backend field rename would fail silently with no compiler error.
- **Home screen `activeOrderCount` is hardcoded to 0** (found via QA-audit cross-check, 2026-07-16). `app/(tabs)/home.tsx:109` — `const activeOrderCount = 0;`, passed straight into `StoreStatusCard`. Shopkeepers see a permanently-wrong "0 active orders" indicator regardless of actual order volume.
- **Store images support only a single image, no carousel/array** (found via QA-audit cross-check, 2026-07-16). `lib/storage.ts:71-77` `uploadStoreImage()` uses a fixed path (`${storeId}/cover.${ext}`) and returns a single result; `app/profile.tsx:53` holds only one `storeImageUri` state value. Real product-completeness gap for customer-facing store listings.
- **No admin notification on profile-edit click (24h approval workflow)** (found via QA-audit cross-check, 2026-07-16). Zero matches anywhere in the repo for a `profile_change_request` concept or any admin-notify call in `app/profile.tsx`. Profile edits take effect with no admin review trail.
- **`toggleStoreStatus` is implemented three separate times, only one is actually wired to the UI** (found via QA-audit cross-check, 2026-07-16). `lib/store-service.ts:146`, `hooks/useStore.ts:75`, and an inline copy in `app/(tabs)/home.tsx:252-275` (renamed `toggleOnline`, the only one actually called by `handleStatusToggle`/`StoreStatusCard`). A fix applied to either of the two unused implementations would silently never take effect — real regression risk for future maintainers editing the "obvious" file.
- **`setAllProductsOffline`/`restoreActiveProductsOnline` are silent no-ops called unconditionally in the online-toggle critical path** (found via QA-audit cross-check, 2026-07-16). `lib/storeProducts.ts:436-438,444-446` both return `true` unconditionally (comment: "no-op now that quantity/in_stock are removed"), yet are still awaited unconditionally in `app/(tabs)/home.tsx:260,267` on every toggle. Toggling a store offline gives shopkeepers the false impression that product visibility actually changes when nothing happens server-side.
- **`lib/orders-db.ts` has a silent 4-strategy fallback chain, all failures swallowed to `console.warn`, none surfaced to the UI** (found via QA-audit cross-check, 2026-07-16). `getOrdersFromDb()` cascades RPC → direct-table (90-day/100-row cap) → `customer_orders` fallback, imported by `previous-orders.tsx`, `invoice/[orderId].tsx`, `payments.tsx`. An RLS misconfiguration or RPC failure degrades silently into an indistinguishable "no orders" empty state.
- **App's own `supabase/*.sql` scripts are tracked in git but aren't real Supabase-CLI migrations — unverifiable whether ever applied to the live DB** (found via QA-audit cross-check, 2026-07-16). Confirmed tracked via `git ls-files supabase/` (contradicting an earlier claim they were untracked), but there's still no `supabase/migrations/` directory or `config.toml` in this repo — several live code paths (RLS policies, RPC functions referenced by the `orders-db.ts` fallback chain above) depend on these having actually been run against production, with no static way to confirm.

### Low

- Several silent `catch {}` blocks in approval-check/session flows (already partly known/intentional, re-confirmed present).
- Heavy unconditional `console.log`/`warn`/`error` in `lib/notifications.ts`, not gated behind `__DEV__`, including full notification payload contents.
- `scheduleNotification()`'s `_triggerDate` parameter is entirely ignored — always fires immediately regardless of what's passed, misleading if a "daily summary" feature (listed as a settings option with no backing implementation) is ever built on top of it.
- **`registration-success.tsx` always redirects to Home after a flat 2.5s regardless of outcome** (from 2026-07-15 audit) — no immediate "you're pending" moment right after signup.
- **`isActivated` field defaults to `true` when missing from the server response** (from 2026-07-15 audit) — fail-open, though it currently only affects a profile-page label, not access control.

---

## Rider app (`NAT_Near-Now_Rider-`)

### Critical

- **Supabase service-role key is plumbed into the client bundle via `app.config.js`.** `constants/config.ts:33-37` + `app.config.js:86-90` — if `EXPO_PUBLIC_SUPABASE_SERVICE_ROLE_KEY` is ever set at build time (an easy mistake alongside the anon key), the full-privilege key ships inside every installed APK/IPA, extractable via decompilation. Nothing in the app currently *uses* it, but the plumbing that would ship it exists.

### High

- **Unauthenticated fallback leaks every rider's PII.** `app/(tabs)/profile.tsx:110-135` — if `/delivery-partner/profile` doesn't return success, falls back to fetching the *entire* delivery-partner roster (`/api/delivery/partners`) with no token, exposing every other rider's verification documents, phone, and address just to client-side `.find()` the current user's own record.
- **App requests "Always" location permission but never actually does background tracking.** `app.config.js` configures `NSLocationAlwaysUsageDescription` and the always-permission plugin, but the code only ever calls `requestForegroundPermissionsAsync()` and uses foreground-only `watchPositionAsync` + a JS `setInterval` heartbeat — both of which stop firing when the app is backgrounded/locked. A rider mid-delivery with a locked phone has stale location in `driver_locations` while still being dispatched new offers against that stale position. Also a plausible App Store review risk (declaring an unused permission purpose).
- **Reinstatement from suspended/offboarded is invisible until app restart.** The status-approval poll only starts when `driverStatus === "pending_verification"` — a rider who was `suspended` when the app loaded, then reinstated by an admin while the app stays open, has no mechanism to discover the change short of force-quitting and relaunching.
- **Approval detection during `pending_verification` is poll-only (30s interval), no realtime subscription on the rider's own profile row** (from 2026-07-15 audit) — unlike order offers, which do use Realtime for near-instant delivery. A newly-signed-up rider waits up to 30s after admin approval to see it reflected, rather than instantly.
- **No `ErrorBoundary` anywhere in the app** (from 2026-07-15 audit) — an unhandled render exception crashes the whole app with no fallback UI.

### Medium

- Double-tap on "Accept offer" can fire two concurrent accept requests before React's `disabled` state re-render lands — no `useRef` in-flight lock, only async state (server-side atomicity likely prevents double-award, but produces duplicate network calls/alerts/navigations).
- A realtime-channel subscribe/teardown race (`home.tsx:321-356`) can orphan a Supabase channel subscription if `isOnline` toggles quickly between the effect running and its async session-fetch resolving — leads to duplicated haptic buzzes/fetches for new offers over a session.
- Rider-facing "Earnings" screen hardcodes a 15% commission client-side in two separate places (`profile.tsx:147`, `earnings.tsx:90-98,163,217`) with no backend-provided actual payout field — will silently drift from real payouts if the commission structure ever varies by store/promo or changes.
- "Ignore offer" is client-state-only, unpersisted — resets on any app restart, so a previously-ignored offer can reappear.
- **Active-order poll runs every 6s regardless of online/offline status** (found via QA-audit cross-check, 2026-07-16). `app/(tabs)/home.tsx:319-342` — gated only on `!token`, unconditionally calls `fetchActiveOrder()` + `setInterval(fetchActiveOrder, 6000)`. Only the *offers* poll is gated on `isOnline`; a code comment justifies the active-order poll running regardless ("a simulation or manual dispatch can assign an order even when offline"), but it still means unnecessary battery/network drain for riders who are deliberately offline.
- **No admin notification/audit trail on rider profile-edit requests** (found via QA-audit cross-check, 2026-07-16). `app/(tabs)/profile.tsx:212-244` (`handleSave`) does a direct `PATCH /delivery-partner/profile` with no request-tracking or admin-facing notification. Profile changes (name/email/address) take effect immediately with no admin visibility or approval workflow.
- **Rider rating hardcoded at 4.8** (found via QA-audit cross-check, 2026-07-16). `app/(tabs)/profile.tsx:354` — literal `4.8`, no backend-sourced field. Displays a fabricated trust/quality signal that has no relation to actual performance.
- **No vehicle type/image fields anywhere in the rider profile UI** (found via QA-audit cross-check, 2026-07-16). Backend already returns `vehicle_number` but nothing in `app/(tabs)/profile.tsx` displays it — incomplete KYC/profile data surface for riders and admins reviewing rider identity.
- **No document image upload for rider verification** (found via QA-audit cross-check, 2026-07-16). `app/(tabs)/profile.tsx:369-388` — "Verification Document"/"Verification Number" render as plain read-only text, no image picker/upload UI. Riders have no way to submit/update KYC verification documents from the app.
- **Orders screen status badges mismatch what the backend actually emits** (found via QA-audit cross-check, 2026-07-16). `app/(tabs)/orders.tsx:30-34` defines a badge for `en_route_delivery`, but `deliveryPartner.controller.ts`'s `mapDbStatusToRider()` (71-81) only ever returns `rider_assigned`/`picking_up`/`picked_up`/`completed` — `en_route_delivery` is dead, and `picking_up`/`picked_up` (which the backend does emit) have no badge config, falling through to unstyled generic rendering.

### Low

- `lib/authService.ts` is entirely dead code (never imported), and its unused OTP functions log raw phone numbers and API URLs — latent PII-in-logs risk if ever wired back in.
- `components/MapViewWrapper.tsx` is unused dead weight — the app bundles `react-native-maps` and Google Maps keys for no actual in-app map usage (only deep-links out to the native Maps app).
- Same "state instead of ref for in-flight-request guard" pattern recurs in `otp.tsx`'s verify flow as in the accept-offer bug above — low-likelihood double-submit race.
- `isActivated` session field continues to be threaded through every session-save call site despite being confirmed dead/unused elsewhere — easy to mistake for the real approval gate.
- **Hardcoded admin support phone number baked into `home.tsx`** (from 2026-07-15 audit), rather than sourced from config.

---

## Customer app (`nearandnowcustomerapp`)

### Status of 6 previously-known issues
(logout doesn't clear cart/push token; ~~`apiClient.ts` 401 handling doesn't clear session~~ — **FIXED 2026-07-16, see below**; `trackingService.ts` treats any failure as "Order not found"; cold-start push deep links can be dropped; no push-token unregister on logout; dead "Order Placed!" overlay in checkout.tsx — the other 5 confirmed still present, unfixed)

**Session model, clarified and implemented 2026-07-16:** intent is "stay logged in until explicit logout, or until 25 days of real inactivity — not 25/30 days since the original login." Two things were needed and both are now done:
- **Backend (`customerAuth.middleware.ts`):** `SESSION_TTL_MS` was a *fixed* 30-day window from `session_token_issued_at` (set once, at login) — a daily user would still get logged out on day 31 regardless of activity. Changed to 25 days and made it a genuine sliding window: every single authenticated request renews `session_token_issued_at` to the current time (fire-and-forget, doesn't block the response) — precise to the request, not throttled to a daily/coarser granularity, so the 25-day clock always counts from the customer's actual last activity down to the second. A customer who opens the app at least once every 25 days never gets logged out; 25 days of zero requests lets the token actually expire. (An earlier version of this fix throttled the renewal write to once/day to reduce DB load — reverted in favor of exact precision per an explicit product decision; worth reconsidering if this table's write volume ever becomes a real concern.)
- **App — the 401 handling was fixed, not just the token storage.** Previously `apiClient.ts` threw "Session expired" on a 401 but never cleared the stored token or updated the app's auth state — the customer stayed stuck in a broken "looks logged in, everything 401s" state instead of being dropped back to login. Added a `setSessionExpiredHandler` registration (`apiClient.ts` ↔ `AuthContext.tsx`) so any 401 from any API call — not just ones `AuthContext` itself makes — clears the session and flips `isAuthenticated` immediately, from wherever the user is in the app. Also added a global redirect in `app/_layout.tsx` (`AppShell`) watching the `isAuthenticated` true→false transition and sending the user to `/phone` — previously only `app/index.tsx`'s cold-start check redirected, so a session expiring mid-use (deep in checkout, tracking, etc.) had no path back to login at all.
- ~~**Bug found and fixed the same day: renewal only fired on specific ordering-related API calls, not on general app usage.**~~ — **FIXED.** Traced every code path and found the sliding-window renewal only runs inside `requireCustomer`, which only fires when the app hits a `requireCustomer`-gated backend route. Browsing (home/product/search) never does — `lib/productService.ts`/`lib/storeService.ts` talk to Supabase directly with the app's own client, bypassing the backend entirely. Worse, even the app's own "am I still logged in?" check on cold start (`AuthContext.restoreSession` → `getCurrentUserFromSession`) does the same — so simply *opening the app* didn't renew anything either; only placing/tracking an order, or touching addresses/email, happened to renew. A customer who opened the app daily but hadn't ordered in 25 days would still have been logged out — contradicting the stated intent. Fixed by adding a dedicated `GET /api/customers/session/ping` route (`customers.routes.ts`) whose only job is to sit behind `requireCustomer`; the app now calls it fire-and-forget on cold start (`AuthContext.restoreSession`) **and** on every foreground resume (new `AppState` listener in `AuthContext.tsx`), so any real use of the app — not just ordering — renews the session.
- ~~**Two smaller bugs in the renewal code itself, found on review.**~~ — **FIXED.** (1) The fire-and-forget renewal `UPDATE` filtered only by `session_token`, not `role = 'customer'` like the initial `SELECT` does — practically safe given token randomness, but inconsistent; added the same role filter for defense-in-depth, on both the renewal write and the expiry-cleanup write. (2) The expiry-cleanup `UPDATE` (nulling out an expired token) wasn't wrapped in try/catch, and this codebase has no global Express error handler (a separately-tracked open bug, see Backend Medium) — if that specific DB write had failed, the request would hang/crash instead of cleanly returning 401. Wrapped in try/catch so the 401 response always sends regardless of whether the cleanup write itself succeeds.

### Critical (new)

**Open:**
- **The Supabase service-role key is structurally required client-side for `addressService.ts`, `authService.ts`, `product/coupons.tsx`, and `distanceUtils.ts`.** `lib/supabase.ts:31-54` builds an admin client from `SUPABASE_SERVICE_ROLE_KEY || EXPO_PUBLIC_SUPABASE_SERVICE_ROLE_KEY || SUPABASE_ANON_KEY`. Since only `EXPO_PUBLIC_*` vars get inlined into the RN bundle, the non-public `SUPABASE_SERVICE_ROLE_KEY` is `undefined` at runtime on a real device — meaning in production, **either** `EXPO_PUBLIC_SUPABASE_SERVICE_ROLE_KEY` must be set (shipping the full-access key inside every installed app, trivially extractable, giving anyone unrestricted read/write to every table) **or** all address CRUD, login/profile fetch, and coupon-viewing are simply broken. This needs an architecture fix — route these through the authenticated backend API instead of a direct privileged Supabase client — not a config change. (`orderService.ts`'s piece of this — order placement — is fixed; see Fixed below. Do not remove `supabaseAdmin`/`assertSupabaseAdminConfigured` from `lib/supabase.ts` until the remaining 4 files are migrated too.)
- **No session-to-userId binding on `authService.ts`/`addressService.ts` — IDOR.** `authService.ts:133-189`, `addressService.ts` (all CRUD) take a bare `userId` string with zero check it matches the authenticated session's actual identity — the app doesn't even send the session token to these calls. Given the service-role key is (per above) necessarily reachable, anyone can query any other customer's full profile and saved addresses (with phone/lat-lng) by ID alone. (`orderService.ts`'s piece of this — order placement — is fixed; see Fixed below. `orderService.ts:211-253,524-545`, the read-only order-history/status lookups, are unaffected and still direct-Supabase — not fixed.)
**Fixed:**
- ~~**Session tokens stored in plain AsyncStorage, no secure storage.**~~ — **FIXED 2026-07-16** (found via QA-audit cross-check, same day). `context/AuthContext.tsx` was storing `userToken` via plain `AsyncStorage.setItem`/`getItem`/`removeItem` alongside the non-sensitive `userId`/`userData`/`customerData` — extractable by anyone with filesystem access on a rooted/jailbroken device or via device backup extraction. Added `expo-secure-store` (dependency + `app.config.js` plugin entry) and moved just the token (not the other, non-sensitive cached fields) to `SecureStore.setItemAsync`/`getItemAsync`/`deleteItemAsync` — backed by Android Keystore / iOS Keychain, not a plain readable file. All 4 call sites updated: 3 in `AuthContext.tsx` (`restoreSession`, `clearStoredSession`, `verifyOTPCode`), 1 in `lib/apiClient.ts` (the read used to attach the `Authorization` header on every API call). **Downstream check:** installs from before this change have their token sitting in the old AsyncStorage location — without a migration, every already-logged-in user would be silently signed out the moment they update. Added a one-time fallback (check SecureStore first, fall back to the legacy AsyncStorage key, migrate and delete it) in both `restoreSession` and `apiClient.ts`'s token read (the latter to close a race where an API call could fire before `AuthContext`'s migration runs) — existing sessions now survive the update.
- ~~**Order totals are entirely client-computed and written directly via that same service-role-requiring client.**~~ — **FIXED 2026-07-16.** `lib/orderService.ts`'s `createOrder` no longer writes to Supabase directly. It now calls the backend's `POST /api/orders/place` — the same server-trusted pipeline already used by the website, which re-derives every item's price from `master_products`, recomputes the trusted subtotal/floor-checked total, and force-locks `payment_status` to `'pending'` (only `/api/payment/verify`'s signature-checked path can flip it to `'paid'`). Verified the app's own product-browsing (`lib/storeService.ts`'s `getNearbyProductFilter`) already scopes `master_products` results to what's actually stocked by nearby stores via the real `products` table, so cart items built from normal browsing resolve cleanly through `/api/orders/place`'s stricter store-matching — the old direct-write path's "forgiving fallback" (pin unmatched items to an arbitrary store, stuff them into `notes`) is no longer needed and was removed. `notes` (delivery instructions) and a folded-in GSTIN/tip note are preserved by extending `placeCheckoutSchema`/`placeCheckoutOrder` with an optional `notes` field (backend `orders.routes.ts`, `database.service.ts`) — same free-text column as before, just via a real field instead of the client stuffing it in blind.
- ~~**`orderService.ts`'s IDOR — order placement took a bare `user_id` with no session check.**~~ — **FIXED 2026-07-16.** `POST /api/orders/place` now requires `requireCustomer` (`orders.routes.ts`), and `OrdersController.placeCheckout` (`orders.controller.ts`) always overwrites `req.body.user_id` with `req.customerId` before it reaches `placeCheckoutOrder` — the client-sent `user_id` is now completely ignored, closing the IDOR for this endpoint specifically.

### High (new)

- **Cart is never cleared after a successful order**, COD or paid — confirmed via grep, the only `clearCart()` call site is in the *failed/cancelled* payment branch. A customer can accidentally re-order the same items after checking out successfully.
- **No cart quantity ceiling and no stock check anywhere** — `CartContext.tsx` places no upper bound on quantity, and `orderService.ts`'s `createOrder` never validates against any inventory field before inserting.
- **`discount_amount` is hardcoded to `0` on every order** regardless of an applied coupon (`orderService.ts:375`) — the discount is folded into `total_amount` before being sent, but the dedicated column that finance/reporting would read is always wrong.
- **Coupon expiry is fetched but never enforced**, client or server side — an expired-but-still-`is_active` coupon (or a stale cached/tampered coupon object) remains applicable with zero re-validation at order-creation time.
- **Google Maps key called directly from the device, bypassing the backend's own proxy** (found via QA-audit cross-check, 2026-07-16). Confirmed direct calls to `maps.googleapis.com` with `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` at `app/profile-setup.tsx:130`, `app/location/select-map.tsx:134,216,287`, `app/location/add.tsx:149,168`, `app/location/edit.tsx:115,132`. `backend/src/routes/places.routes.ts` already provides an equivalent proxy and is never called from this app — ships a billable, extractable Google Maps API key inside every installed app instead of routing through the backend's rate-limitable proxy (the backend proxy's own lack of rate-limiting is already tracked under Backend Medium — this finding is that the customer app bypasses it entirely, a distinct gap).
- **Invoice screen is entirely fabricated client-side; PDF download is a stub** (found via QA-audit cross-check, 2026-07-16 — broader than the already-tracked placeholder-GSTIN Low item for the same general area). `app/order/invoice/[id].tsx:38-54` builds a fake invoice object from the locally-fetched order list, never calling the real `GET /api/invoices/order/:orderId/customer`. `handleDownload` (~111-119) shows an alert that PDF download is "coming in next update." Customers cannot get a real tax invoice for their order from the app at all.

### Medium (new)

- Widespread fire-and-forget `.catch(() => {})` patterns across cache writes and order-placed flags — individually low-risk, but no logging/telemetry means cache desyncs are invisible in production.
- Order-tracking screen runs a 5s fallback poll and a 2s driver-location poll unconditionally alongside the realtime channel, doubling backend load for no benefit when realtime is healthy.
- Order-confirmation screen finds the just-placed order by scanning the user's full order list client-side rather than fetching it directly — degrades to a silent empty state with no error/retry if the new order hasn't propagated into the (possibly cached) list yet.
- GSTIN input in checkout has no format validation before being persisted and later printed on the customer's own tax invoice.
- **Wallet screen is a fully non-functional shell** (found via QA-audit cross-check, 2026-07-16). `app/wallet.tsx:37-45` — "Add Money" just shows a "coming soon" alert; balance is hardcoded to `₹0.00`. A customer-facing feature that's advertised in the UI does nothing.
- **Saved-payment-methods feature is unimplemented end-to-end** (found via QA-audit cross-check, 2026-07-16). `app/support/payment-options.tsx:112-137` — the screen's own code comment confirms there's nothing to display until the backend wires up Razorpay Customer tokens; only renders an empty state.
- **Payment reconcile loop has no circuit breaker for failed/cancelled payments** (found via QA-audit cross-check, 2026-07-16). `hooks/usePaymentFlow.ts:59-73` — `reconcile()` loops for the full 10s window and only short-circuits early on `'paid'`, never on `'cancelled'`/`'failed'`. A customer whose payment was clearly cancelled waits out the full window before the UI reflects reality.
- **Hardcoded flat ₹25 delivery fee, distance data computed but discarded for pricing** (found via QA-audit cross-check, 2026-07-16 — the customer-app twin of the already-fixed website flat-fee bug). `constants/fees.ts:3,7-9` — `DELIVERY_FEE = 25` constant; `lib/distanceUtils.ts`'s real distance computation is only used for a UI warning in `checkout.tsx`, never fed into the fee calculation. Customers on far-away orders are undercharged relative to the site's distance-tiered pricing.

### Low (new)

- 29+ more `console.log`/`warn`/`error` call sites beyond what's already known, no structured error tracking.
- `lib/supabase.ts` logs a "⚠️ CRITICAL" warning string to the on-device console in production if credentials are missing, rather than failing fast/reporting to a tracker.
- `invoiceService.ts` hardcodes literal placeholder GSTIN/phone strings (`'29XXXXXXXXXXXXX'`) as a silent fallback if business-info fetch fails — could ship on a real customer invoice.
- Biased shuffle (`Array.sort(() => Math.random() - 0.5)`) used for "suggested products" — harmless here, but a correctness smell if the pattern is copied elsewhere.

---

## Build / test / lint infrastructure — deep dive (2026-07-16, later pass)

Ran `tsc --noEmit`, the test suite, and lint across all 7 areas (backend, frontend, admin, and all three mobile apps) and root-caused every failure — not just symptom-listed. This is about broken *tooling* (things that should catch bugs but currently don't, or fail for reasons unrelated to the code they're checking), separate from the functional/security bugs above.

**Clean, no issues found:** backend `tsc`, frontend `tsc`, admin `tsc`, store-owner app `tsc` + full Jest suite (18/18 passing).

### Backend

**Open:**
- **Both `createPaymentOrder` tests in `payment.service.test.ts` still fail** with `res.text is not a function`. This is unrelated to the dotenv issue below: the tests call `vi.stubGlobal('fetch', fetchMock)` intending to mock Razorpay's own HTTP call, but `vi.stubGlobal` replaces `fetch` process-wide — so `PaymentService.createPaymentOrder`'s *earlier* call to `databaseService.getOrderPaymentContext()` (a real Supabase query) also gets intercepted by the same mock, whose shape (`{ ok, json }`) doesn't implement `.text()`, which `@supabase/postgrest-js` calls internally. Needs the test to either mock the Supabase call separately (e.g. `vi.spyOn` scoped to the Razorpay URL only) or restructure the mock to implement a full `Response`-shaped object.

**Fixed:**
- ~~`payment.service.test.ts` had 4 failing tests (`verifyPayment`, `verifyWebhook`, both `createPaymentOrder` cases)~~ — **2 of 4 FIXED 2026-07-16** (`verifyPayment`, `verifyWebhook`; the other 2 are the separate, still-open issue above). Root cause was a test-isolation bug in `src/config/database.ts:10-13`, which did:
  ```
  dotenv.config({ path: '../../.env' });
  dotenv.config({ path: '../../../.env', override: true });
  dotenv.config({ path: cwd + '/.env', override: true });
  ```
  The `override: true` calls unconditionally overwrote already-set `process.env` values from whatever's in the real root/backend `.env` files (both exist locally with real secrets). The tests manually set `RAZORPAY_KEY_SECRET`/`RAZORPAY_KEY_ID` *before* dynamically importing `payment.service.ts` (via `vi.resetModules()` + `await import(...)`, specifically to control this) — but `payment.service.ts` imports `supabaseAdmin` from `config/database.js`, whose top-level `dotenv.config({ override: true })` calls fired on that same import and silently clobbered the test's values with the real `.env`'s. Confirmed by reproducing outside vitest: manually setting the same env vars and calling `verifyPayment` with a correctly-computed signature still returned `false`. **Impact beyond these tests:** any test anywhere in the backend that manually sets an env var and then imports anything transitively touching `config/database.ts` (most of the app) would have that value silently overwritten — a systemic test-isolation hole, not specific to payment tests. **Fix:** `config/database.ts`'s two `override: true` dotenv calls now skip themselves when `process.env.NODE_ENV === 'test'` (which vitest sets automatically) — dev/production behavior is byte-for-byte unchanged, this only affects `vitest run`. Also found and fixed a second, unrelated one-line bug while verifying: `verifyWebhook`'s test signs its payload with a `TEST_SECRET` but `setRazorpayEnv()` never actually set `RAZORPAY_WEBHOOK_SECRET` in `process.env` — added it.

### Frontend

**Open:**
- **`invoice.test.ts`'s "downloads customer invoice as html file" test fails** (`expected "revokeObjectURL" to be called at least once`) — a genuine test bug, not a product bug: `src/utils/invoice.ts:108-122`'s `downloadHtml()` wraps `URL.revokeObjectURL(url)` in `setTimeout(..., 100)`, and the test asserts immediately after calling the function with no fake-timer advance or await — the assertion runs before the 100ms callback ever fires. The actual download behavior in the app is unaffected. Fix: `vi.useFakeTimers()` + `vi.advanceTimersByTime(100)`, or await a real 100ms.

**Fixed:**
- ~~No `vitest.config.ts` and no `test` block in `vite.config.ts` existed — vitest ran with bare defaults (`environment: 'node'`, `globals: false`), and `jsdom` wasn't a direct devDependency.~~ — **FIXED.** This caused `Button.test.tsx`'s whole suite to fail to even run (`describe is not defined`, no globals) and 3/4 of `ProductCard.test.tsx` to fail with `document is not defined` (no jsdom). Added `frontend/vitest.config.ts` (`environment: 'jsdom'`, `globals: true`, wired up the pre-existing-but-never-included `src/test/setup.ts` via `setupFiles` — that file already had jsdom-appropriate mocks for `matchMedia`/`IntersectionObserver`/`localStorage` written and ready, just never activated by any config). Added `jsdom` as a real `devDependency` (previously only resolvable by luck via `vitest`'s own optional peer dependency being incidentally present locally — not something a fresh `npm install` would have guaranteed).
- ~~Making tests actually execute (the fix above) surfaced 3 previously-invisible bugs~~ — **all 3 FIXED:**
  - `Button.test.tsx`: 7/8 failed with "multiple elements found" for `getByTestId('button')`. The file imported `render`/`screen` from `@testing-library/react/pure` instead of the normal `@testing-library/react` entrypoint — `/pure` deliberately opts out of the automatic `afterEach(cleanup)` that unmounts the DOM between tests, so every test's rendered button piled up in the document instead of being removed. Swapped the import to `@testing-library/react`. All 8/8 now pass.
  - `ProductCard.test.tsx`: all 4 failed with `Cannot destructure property 'basename' of 'React__namespace.useContext(...)' as it is null` — `ProductCard` renders a react-router `<Link>` internally, but the test's `renderWithContexts` helper (and one raw `render()` call) only wrapped it in `NotificationContext`/`CartContext` providers, not a `<MemoryRouter>`. Added the `<MemoryRouter>` wrapper to both.
  - `ProductCard.test.tsx`'s "calls addToCart" test additionally expected `showNotification` to fire with `"Test Product added to cart"` — found to be testing a feature that doesn't exist, not a test-infra bug. Confirmed via `grep -rn "added to cart" src/` that this string appears nowhere in actual app source — neither `ProductCard.tsx` nor `CartContext.tsx`'s real `addToCart` ever calls `showNotification`. Per user decision, removed the assertion rather than adding a new toast feature or leaving a permanently-failing test; `addToCart` itself is still asserted and covered.
  - Net: **16/17 tests now pass** (up from an undercounted 8 total before the environment fix, since the crashed `Button.test.tsx` file's individual tests weren't even enumerated). The 1 remaining failure is `invoice.test.ts`'s `setTimeout` timing bug above — not touched in this pass.

### Admin panel — no test infrastructure exists

`admin/package.json` has no `test` script and no test files were found. Not a "failing test" in the technical sense, but worth flagging given this is the app that gates every store/rider approval and holds the (already-flagged-Critical) client-side admin login — zero automated coverage on the most security-sensitive surface in the codebase.

### Rider app (`NAT_Near-Now_Rider-`)

`tsc --noEmit` clean. `expo lint` (previously never run — no lint had ever been configured, `expo lint` auto-generated a fresh ESLint config on this run) found:
- **2 real errors**: `components/MapViewWrapper.tsx:14,20` — both component definitions are missing a `displayName` (`react/display-name`). Low real-world impact since this file was already flagged in the audit above as unused dead weight (bundles `react-native-maps` for a feature that only deep-links out to the native Maps app).
- **18 warnings**, all `react-hooks/exhaustive-deps` (missing animation-ref dependencies in `useEffect`/`useCallback` across `otp.tsx`, `signup.tsx`, `home.tsx`, `orders.tsx`, `profile.tsx`, `_layout.tsx`, `index.tsx`, `phone.tsx`, `earnings.tsx`, `delivery/[orderId].tsx`, `OfflineBanner.tsx`) plus 2 `no-require-imports` warnings. Cosmetic/intentional in most cases (animation values are refs that shouldn't retrigger effects) — not investigated file-by-file, flagged for awareness only.
- No `test` script/infrastructure exists in this app either.

### Customer app (`nearandnowcustomerapp`)

~~`tsc --noEmit` had 2 errors~~ — **both FIXED 2026-07-16:**
1. ~~**`app/_layout.tsx:10` — `hooks/usePushNotifications.ts` has no exported member `usePushNotifications`.**~~ — **FIXED.** That file was a platform-switch shim written as raw CommonJS (`module.exports = require('./usePushNotifications.dev')` / `.expo-go`), not an ES `export` — TypeScript couldn't see through the `module.exports =` reassignment. Rewritten to a proper `export const usePushNotifications: PushHookType = isExpoGo ? require(...) : require(...)`, typed via `typeof import('./usePushNotifications.dev').usePushNotifications` (a type-only reference that doesn't execute the module). Deliberately kept the runtime `require()` switch rather than static imports: `usePushNotifications.dev.ts` calls `Notifications.setNotificationHandler(...)` at module top-level, which must never execute in Expo Go — static imports would have evaluated both branches unconditionally and broken that.
2. ~~**`app/otp.tsx:88` — `router.replace({ pathname: "/verify-email", ... })` rejected by Expo Router's typed-routes union.**~~ — **FIXED.** Confirmed this was exactly the stale-local-cache issue suspected: the generated type manifest (`.expo/types/router.d.ts`) is gitignored/machine-local and only regenerates when the Metro dev server resolves the `app/` directory. Booted `expo start` briefly (killed once typegen completed, ~20s) — `/verify-email` is now correctly present in the generated route union. No code change; confirms this was never a real bug in the committed source, just a stale local artifact.

`tsc --noEmit` is now fully clean for this app.

`expo lint` **fails outright for the entire app** (`No files matching the pattern ".../src" were found` — 0 files ever scanned). Traced to a stray, empty `src/screens/` directory on this machine: `git ls-files src` returns nothing (never tracked) and `git log -- src` shows only a large prior cleanup commit, so this is local leftover cruft, not a repo-wide issue — but `expo lint` apparently defaults to scanning `src/` when the directory is present, regardless of whether real source lives there (it doesn't; the actual code is under `app/`, `lib/`, `hooks/`, `components/`, `context/`). Anyone with the same stale empty directory will have lint silently no-op. Fix: delete the empty local `src/` directory.

No `test` script/infrastructure exists in this app either — same gap as admin and the rider app.

---

## Suggested order of attack

Given the sheer volume, this is not a "fix everything in one pass" list. Reasonable sequencing, updated 2026-07-16 (later pass, then again after a QA-audit cross-check pass added 58 new findings from `QA_AUDIT.md`/`qa_audit_old.md`) to reflect what's already closed:

1. ~~Money-losing bug live right now~~ — **done**: unauthenticated refund endpoint (backend), client-trusted checkout totals, rider order-hijack, order-placement auth/IDOR (backend + customer app), OTP-signup-verification bypass (backend + store-owner/rider apps).
2. **Customer app service-role-key architecture fix — the remaining 4 files.** `addressService.ts`, `authService.ts`, `product/coupons.tsx`, `distanceUtils.ts` still need the same treatment order placement already got (route through the authenticated backend). This is the biggest open item — unblocks the remaining IDOR gaps under it too. The QA-audit pass added one more reason to prioritize this: `authService.ts`'s reliance on the anon client is also why `getCurrentUserFromSession` is broken.
3. **Auth bypass still open**: admin client-side login (admin panel) — compounded by the QA-audit finding that `admins` has no RLS policy at all and admin creation has no server-side role check, so this is now two related Critical gaps, not one.
4. **PII/abuse gaps**: tracking-endpoint cross-customer PII leak (backend) — still open. ~~pickup/delivery OTP brute-forceability (backend)~~ — **done** 2026-07-16, rate-limited.
5. **New from the QA-audit cross-check, worth pulling forward despite being newly-found rather than newly-introduced:** the admin panel's "Send Push Notification" feature is a complete no-op that reports false success to admins (Admin High) — actively misleading, cheap to fix or disable; the customer mobile app's session tokens sit in plain AsyncStorage with no secure storage (Customer app Critical) — same severity class as the service-role-key issue in #2, arguably simpler to fix first.
6. Then work down through High → Medium → Low per area as time allows — see each area's **Open** subsections above (Fixed items are now separated out so this is easier to scan). The QA-audit pass roughly doubled the size of several sections (Admin panel, Website frontend, all three mobile apps) — expect more Medium/Low volume there than a first read of this doc's history would suggest.
6. **Tooling, lower urgency but cheap**: `invoice.test.ts`'s timing bug and `createPaymentOrder`'s test-mock bug (both frontend/backend test-only, small fixes); admin/rider/customer apps have zero automated test coverage — worth at least a starter suite given admin gates approvals and rider/customer apps handle money; customer app's `expo lint` is fully broken (stray local `src/` dir) and has 2 `tsc` errors (one real — the CJS shim — one likely a stale local cache).
